# DataWizard 작업

## 데이터 클렌징 3대 요구사항 (완료)

### 1. 여러 컬럼 → 한 컬럼 합치기 (공백/구분자 지정)
- [x] `ColumnSourceSheet` 구분자 빠른 선택(붙여쓰기/공백/하이픈/쉼표) 칩 추가
- [x] 파일별 다중 컬럼 조합 (기존)
- [x] 조합 구분자 지정 (기존)

### 2. 정규식으로 한 패턴 통일 + 실패값 표시
- [x] 전화번호/날짜 리뷰에 "표준화 실패" 전체 목록 + 인라인 수정 (`FlaggedValuesBody`)
- [x] `RegexCleanupSheet`에 목표 패턴(검증) 입력 → 맞지 않는 값(실패) 목록
- [x] 전화번호 정규화 `1023755880`/`+821023755880` → `010-2375-5880` (기존 Normalizer)

### 3. 매핑테이블 추가 → 전 데이터 매핑
- [x] 매핑표 붙여넣기/가져오기 파서 (`MappingTableParser`, 탭·→·:·쉼표 구분)
- [x] 매핑표 가져오기 시트 + 적용 결과 미리보기 (`MappingTableSheet`)
- [x] 미매핑 커버리지 표시 (전체 N종 / 매핑 M종 / 미매핑 K종) + "미매핑 값 채우기"
- [x] 값별 수동 매핑·자동 통일 (기존)

## 애플 아카데미 데이터 스펙 대응 (간편 Public/Private + 일반)
- [x] 지원방식 3분류: 일반/간편(Public)/간편(Private) — 파일명 기준 (`FilePlan.applicationType`)
- [x] 해외 전화번호 원본 유지 (`Normalizer.cleanPhone`, 비표준은 원본)
- [x] 중복 Key = 전화번호 OR 이메일 (union-find, `MergeEngine.deduplicate`)
- [x] 매핑표 파일 4종 제공 (~/Downloads/for Leeo/mappings/: City/Country/CurrentStatus/LevelOfEducation)
- [x] 스키마를 최종본과 동일한 73컬럼으로 확장 (`UnifiedColumn`)
- [x] CSV 중복 헤더 버그 수정 — "이름 (2)" 접미사로 보존 (`CSVParser`)
- [x] Unique ID 생성 6F10001~ (Code 없는 간편지원, `MergeEngine`)
- [x] 만 나이 + Age Group (기준일: 다음 3/1, `Normalizer.age/ageGroup`)
- [x] Current City ← 도/시 + City(Overseas) 분리, 해외 국가명 치환 (`compose`)
- [x] Current Status = 대학단계+그외 조합, School/Company 3컬럼 조합 시드
- [x] 성별 자동 통일 영문(Male/Female)
- [x] 중복 유지 기준: 최신 Submitted At → 채널 우선 (정답과 채널 분포 일치 검증됨)
- 검증 결과: Submitted 필터 111 ✓ · 총 336행 ✓ · 중복 17그룹/18삭제 ✓ · Keep 채널 12/4/1 ✓

## 명시적 변환 제안 (자동변환 → 제안+승인)
- [x] 전화번호/생년월일: 전체 종을 `이전 값 → 제안 값` 표로 표시 (`ProposalsBody`)
- [x] 요약 칩: 변환 제안 N종 · 이미 표준 N종 · 확인 필요 N종(미해결 카운트)
- [x] 세그먼트 필터 (전체/변환 제안/이미 표준/확인 필요)
- [x] 확인 필요 행만 인라인 수정 가능, 숨겨진 변환 없음

## 매핑표 잠금 + 유사도 추천
- [x] 매핑표 적용 시 컬럼 값을 허용 목록으로 잠금 (자유 입력 → Picker)
- [x] 매핑표 밖 값: 유사도 추천 (`Similarity` — 편집거리 + 한↔영 사전 브리지)
- [x] 행별 "추천: X (NN%)" 승인 버튼 + "추천대로 승인" 일괄 버튼
- [x] 매핑표 시트: 선택 메뉴 유사도순 정렬 + ✨추천 + "추천대로 모두 배정"
- [x] 빈 통일 값(`값 → `) 거부 — 데이터 삭제 사고 방지

## 검증 가능성 (사용자가 100% 신뢰)
- [x] 변경 추적: 병합이 수정한 모든 셀 기록 (`ChangeRecord` — 파일·출처 키·이전→이후)
- [x] 변경 보고서 CSV 내보내기 (원본과 1:1 대조용)
- [x] 결과 화면 검증 요약: "값 수정 N건" / "0건 — 원본 그대로"
- [x] 검토 부제목에 "✏️ 수정 예정 N종" 표시 (합치기 전 확인)
- [x] 매핑표 시트 커버리지 게이지 (미매핑 N종)

## 결정 TODO 흐름 (파일 3개 → 결정할 일만 뜨고 하나씩 해결 → 완성본)
- [x] 검토 화면에 탭 추가: `결정 TODO`(기본) · `전체 컬럼 N` (`reviewTabBar`, `ReviewTab`)
- [x] `결정 TODO` = 실제 판단이 필요한 컬럼만 추림 (`isDecisionRelevant` — 전화/생년월일 표준화 실패, 매핑표 밖 값, 이상값, 값 통일 후보)
- [x] 미해결(⚠️) 항목 위로 정렬 (`decisionReviews`) + `결정해야 할 일 X / Y` 진행 막대 (`decisionProgressHeader`)
- [x] 각 TODO 인라인 해결 (값 편집 → `openCount` 0) 또는 `이대로 확정` 체크(`checked`)로 해결 (`isResolved`)
- [x] 병합 게이트를 `canMerge`(모든 결정 해결)로 변경 — 73컬럼 전부 체크 강제 해제
- [x] 서브타이틀 상태 뱃지(⚠️ 결정 필요 N종 / ✅ 해결됨) + 결정 없을 때 안내 카드
- [x] 해결할수록 미리보기 윈도우에 완성본이 채워짐 (기존 `refreshPreview` 재사용)
- [x] `여러 값을 하나로 모으기` 시트 2단계화 (① 규칙 입력 → ② 적용 결과 미리보기, `stepBar`)
- [x] 명사형 라벨 → 용도형 라벨 (매핑표→여러 값을 하나로 모으기, 정규식→패턴으로 정리하기 등)

## 파일 추가 후 ‘남길 컬럼 고르기’ 단계 (파일 → 컬럼 → 검토 → 결과)
- [x] `Stage.columns` 추가 — 파일 로드 직후 최종 컬럼 선택 화면 (`columnsStage`)
- [x] 스키마 73컬럼을 데이터 있는 것 위 / 빈(자리만) 것 아래로 정렬 + 상태 뱃지 (`columnStatus`)
- [x] 빠른 선택: 데이터 있는 것만 / 전체 선택 / 전체 해제, 선택 N/73 카운트
- [x] 기본값 = 데이터 있는 컬럼(`Set(finalColumns)`), 파일 재로드 시 유효 선택 유지
- [x] 선택(`includedColumns`)이 검토·미리보기·내보내기에 모두 반영 (`visibleFinalColumns`/`visibleReviews`/`includedOrdered`)
- [x] `Exporter.makeCSV/write`에 `columns:` 인자 추가 — 선택된 컬럼만 스키마 순서로 출력
- [x] 이전 완성본을 ‘참조 파일’로 불러와 컬럼 구성 맞추기 (`loadReference`/`applyReference`)
      - 완성본 헤더 ↔ 스키마 컬럼명 대응, 데이터 없어도 참조 컬럼 유지, 참조 컬럼 맨 위 정렬 + ‘참조’ 뱃지
      - 예: 2분기 보고서 + 7·8·9월 데이터 → 2분기와 같은 컬럼 구성으로

## 멈췄다 이어서 하기 (세션 저장/복원)
- [x] `SessionStore` + `SessionSnapshot` — 파싱 데이터(plans)+모든 결정을 Application Support에 JSON 저장
      (파일 재접근 불필요 → 원본이 옮겨져도 복원됨), Codable 라운드트립 검증 완료
- [x] 자동 저장 (0.8초 디바운스) + 창 내리거나 앱 벗어날 때 즉시 저장 (`scenePhase`)
- [x] 파일 화면 상단 ‘이전 작업 이어서 하기’ 카드 (요약·저장 시각 · 이어서/새로 시작)
- [x] 복원 시 stage·plans·finalColumns·reviews·valueMap·allowed·checked·참조 모두 되살림
- [x] 최종 내보내기 완료 시 세션 정리 (`SessionStore.clear`)
- [x] Xcode 프로젝트에 `SessionStore.swift` 등록 (pbxproj, ID 충돌 수정)

## 공식 매핑표 내장 (사양서 Mapping Table)
- [x] `MappingPresets.swift` — Current Status / City(도·시) / Country 공식 한글→영문 내장
- [x] ‘여러 값을 하나로 모으기’ 시트에 `공식 매핑표 채우기` 버튼 (해당 컬럼일 때만)
      - 이미 규칙 있으면 겹치지 않는 줄만 덧붙임 (`fillPreset`), 채운 뒤 2단계에서 검토
- [x] 사양서를 저장소 문서로 보존: `docs/데이터_클리닝_사양.md`
- 참고: Current Status 빈 값(High School Status)은 자동 매핑 제외 — 수기 확인 대상

## 컬럼 고르기 진입 시 ‘틀 있음/없음’ 갈림길 (옵션을 물어보며 진행)
- [x] `ColumnMode`(withTemplate/fromScratch) + `columnMode` 상태 — nil이면 갈림길 화면
- [x] `columnForkView` — “맞출 통합본 틀이 있으신가요?” 전용 선택 화면 (큰 카드 2개, `forkCard`)
      - 네: 완성본 불러오기 → 참조 로드 → **컬럼 선택 건너뛰고 바로 값 검토로** (`chooseTemplate`)
      - 아니요: 남길 컬럼 직접 골라 새 틀 만들기 (기존 `columnPickerView`)
- [x] `loadReference`/`applyReference` → `Bool` 반환 (취소·오류면 갈림길에 머무름)
- [x] 뒤로가기 흐름: 파일 → 갈림길 → (뒤로) 갈림길 → (뒤로) 파일 (`backToFiles`, 피커 ‘← 뒤로’)
- [x] `prepareReview` 진입 시 `columnMode=nil`로 갈림길부터, `restore`는 참조 유무로 모드 복원(갈림길 생략)

## 컬럼 타입 사용자 지정 (자유입력/범주/포맷 — 자동 판단 기본값)
- [x] `ColumnType`(freeText/category/format) + `FormatPreset`(전화번호/이메일/생년월일/직접) enum
- [x] 검토 카드 헤더에 타입 선택 메뉴(`typeControl`) — 포맷이면 하위 형식 메뉴 + ‘자동’ 뱃지
- [x] `typeOverride`/`formatChoice`/`customFormat` 상태 + 자동 판단 헬퍼(`autoType`/`autoFormat`/`effectiveType`)
- [x] 빌더가 모든 비파생 컬럼에 값·샘플·오타를 채워 타입 전환 시에도 데이터 유지 (`reviews`)
- [x] 포맷 검증 본문 `FormatBody` — 형식 안 맞는 값만 모아 인라인 수정 (이메일·직접 정규식)
- [x] 전화/생년월일은 기존 전용 제안 화면 유지(`usesProposalUI`), 이메일은 기본 포맷으로 자동 판단
- [x] `openCount`/`isDecisionRelevant`/`subtitle`/`body`를 effectiveType 기준으로 재작성
- [x] `RegexCleaner.fullyMatches` 추가(앵커 매칭), 세션 저장/복원에 타입·포맷 반영(옵셔널 필드)

## 다이나믹 폰트 / 글자 크기 설정
- [x] `FontSizeOption` (DynamicTypeSize 매핑) + `@AppStorage` 저장 (`AppSettings.swift`)
- [x] 설정창(⌘,) `SettingsView` — 글자 크기 Picker + 미리보기
- [x] 루트에 `.dynamicTypeSize(...)` 적용 → 앱 전체 시맨틱 폰트 스케일
- [x] 메인 제목 고정폰트 → 스케일되는 `.largeTitle`로 변경

## 미리보기에서 컬럼 작업 상태 시각화
- [x] `ColumnWorkStatus` enum — 더 작업 필요(N종)/확정 완료/남은 결정 없음/손댈 값 없음
      - 판정 순서는 검토 화면 `isResolved`와 동일: ‘이대로 확정’이 미해결보다 우선
- [x] `PreviewModel`에 `openCounts`/`decisionColumns` 추가 + `status(_:)`/`needsWorkColumns`
- [x] `refreshPreview`가 `openCount`/`isDecisionRelevant` 결과를 미리보기로 전달
- [x] 컬럼 머리글(`headerCell`) — 상태 아이콘 + 남은 건수 뱃지 + 상태색 밑줄, 작업 필요면 주황 배경
- [x] 작업 필요 컬럼은 셀 전체에 옅은 주황 배경(개선된 셀 파란색이 우선)
- [x] 상단 요약에 `작업 필요 N컬럼` / `모든 컬럼 작업 완료` 배지 (툴팁에 컬럼 이름 나열)
- [x] 하단 범례 바(`legendBar`) — 네 가지 상태 + 개선된 셀 색 설명
- [x] `formatChoice`/`customFormat` 변경도 `refreshPreview` 트리거 (상태 표시 stale 방지)

## 클렌징 프리셋 확장 + 목표 패턴 먼저 보이기
- [x] `FormatPreset`을 15종으로 확장 (`ValueUnify.swift`)
      - 연락처: 전화번호 / 이메일 / 웹 주소
      - 날짜: 생년월일(yyyy-MM-dd) / 연월 / 연도
      - 숫자: 정수 / 소수 / 금액(콤마) / 퍼센트
      - 이름·텍스트: 한글 이름 / 영문 이름 / 영숫자 ID
      - 기타: 우편번호 / 직접 입력
- [x] `FormatPreset.Group` — 타입 메뉴에서 갈래별 Section으로 묶어 표시
- [x] `hint`를 실제 예시 값으로 (`010-1234-5678`, `name@example.com`), `about` 한 줄 설명 추가
- [x] 타입 뱃지에 목표 예시 노출 — 카드를 펴기 전에도 `포맷 · 이메일 name@example.com`
- [x] `FormatBody`: 목표 형식 배너를 본문 맨 위로 (이름+예시 크게, 정규식·설명은 아래 작게)
- [x] `ProposalsBody`: ‘목표 포맷’ 줄을 요약·필터보다 위로 올리고 배너로 (전화 템플릿 편집 포함)

## 경고(⚠️) 있는 값을 목록 맨 위로
- [x] `ProposalsBody` — 열 때 ⚠️(확인 필요·미해결)였던 값들을 맨 위로 (`raisedFirst`)
- [x] `ValueUnifyBody` — 허용 목록 밖 값들을 맨 위로 + 해당 줄 옅은 주황 배경
- [x] 순서는 `onAppear`에서 한 번만 고정 — 타이핑·선택 즉시 줄이 튀지 않게
      (고친 뒤에도 자리 유지, 심볼만 초록/화살표로 바뀜)
- [x] `DistinctValueList`(값 전체 보기) — 이상값 있으면 `점검 필요 먼저`로 열림, 이후 정렬은 사용자 선택 유지
- 참고: `FreeTextBody`는 이미 점검 박스가 맨 위, `FormatBody`는 안 맞는 값만 나열 — 변경 없음

## 확정 컬럼 초록 배경 (미리보기 셀·머리글 색)
- [x] `PreviewModel.cellTint(_:improved:)` — 컬럼 상태가 셀 배경으로 내려옴
      - 확정 = 초록(손봐서 끝낸 열), 남은 작업 = 주황, 그 외 = 없음
      - 개선된 셀은 배경을 뺏지 않고 파란 굵은 글씨로 표시 (두 정보가 겹쳐도 둘 다 읽힘)
- [x] 머리글 배경도 같은 색 계열로 맞춰 열 전체가 한 덩어리로 읽히게
- [x] 범례 바 — 상태 심볼 4종 + ‘파란 굵은 글씨 = 개선된 셀’ 설명

## 검토 중인 컬럼을 미리보기에서 실시간 강조
- [x] `PreviewModel.focused` 추가 — 지금 펼쳐 놓은 검토 카드의 컬럼
- [x] `ReviewSection.onExpandChange` 콜백 — 펼침/접힘을 상위로 보고 (`onDisappear`도 처리)
- [x] `setFocus(_:_:)` — 다른 카드를 펼치면 강조가 옮겨가고, 접으면 해제 (확정된 카드는 제외)
- [x] 미리보기: 강조 열에 파란 덧칠 + 좌우 세로선 + 머리글 위아래 선 → 표 전체를 관통하는 기둥
- [x] 툴바에 `검토 중: <컬럼>` 칩 — 강조 열이 가로 스크롤 밖에 있어도 무엇을 보는지 알 수 있게
- [x] 범례에 ‘검토 중인 열’ 항목 추가
- 한계: 강조는 **열 단위**입니다. 값 하나에 마우스를 올렸을 때 그 값을 가진 셀만 집어주는 건 별도 작업.
- 참고: 미리보기 창이 열려 있어야 보입니다 (카드를 펼칠 때마다 창을 띄우지는 않음)

## 캐릭터·연출 요소 전면 제거
- [x] `PixelArt.swift` 삭제 (스프라이트 격자·`PixelSpriteView`·`AttackKind`·`PopOnChange`·`SkinToggleButton`)
- [x] `AppSkin` / `@AppStorage("appSkin")` / 설정창 스킨 섹션 / ⌘⇧P 메뉴 명령 제거
- [x] 툴바 스킨 토글 2곳(검토·미리보기) 제거
- [x] 보스 배너·HP 막대·하트·전투 연출(`bossBanner` `battleStrip` `battleHearts` `playBattle`) 제거
      → 결정 진행 표시는 원래의 `ProgressView` 한 줄로 환원
- [x] 깃발 로스터(`questRoster` `plantedFlags`) 제거
- [x] `ReviewSection.openCount` 파라미터 제거 (스킨 전용이었음)
- [x] `project.pbxproj`에서 `PixelArt.swift` 등록 해제
- 유지: 상태 계산(`openCount`/`checked`/`ColumnWorkStatus`), 미리보기 색 표시,
        검토 중 컬럼 강조, 포맷 프리셋 확장 — 캐릭터와 무관한 기능은 그대로.
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 부분 정제 — 만들던 통합본에 이번 컬럼만 이어붙이기
한 번에 73컬럼을 다 끝내지 않고, 지금 손볼 컬럼만 정제해 기존 파일에 덮어쓰는 흐름.
- [x] `BaseSheet` — 만들던 통합본을 **값까지** 통째로 보관 (`BaseSheet.swift`)
      - 헤더 순서·스키마 밖 컬럼(온테점수·수기 메모)을 원본 그대로 유지
      - `BaseSheetLoader.load` — CSV/XLSX 읽어 헤더를 스키마 컬럼명으로 대응
- [x] `PatchEngine.apply` — 새로 정제한 행을 기존본에 이어붙임
      - 행 매칭 = **Code → 전화번호 → 이메일** 순 (전화는 `Normalizer.phoneKey`, 이메일은 소문자 비교)
      - 도구가 만든 Unique ID(6F1…)는 매칭 키에서 제외 — 파일 구성이 바뀌면 번호가 밀려 엉뚱한 짝이 됨
      - 중복 Keep 행 우선 색인, `중복 - 삭제` 행은 신규로 붙지 않음
      - **고르지 않은 컬럼은 한 글자도 안 건드림**, 새 값이 비면 기존 값 유지(`keptBlankCount`로 보고)
      - 기존본에 없는 컬럼은 맨 뒤에 새로 생성(`addedColumns`)
      - 기존본에 없는 사람은 맨 아래 추가 + `이어붙임=신규` 표시 컬럼 (둘 다 토글)
- [x] 갈림길 3분기(`ColumnMode`) — 이어서 채우기(`patchBase`) / 틀만 빌리기 / 새로 만들기
- [x] `Stage.focus` + `focusStage` — ‘이번에 정제할 컬럼 고르기’ 전용 화면
      - 손볼 거리 남은 컬럼(⚠️) 위로 정렬, `기존본에 있음`/`새 컬럼으로 추가` 뱃지
      - `손볼 거리 있는 것만` 빠른 선택 (진입 시 기본값)
- [x] 검토 화면은 고른 컬럼만 (`includedColumns = focusColumns`), 툴바에 반영 대상 파일명 표기
- [x] 미리보기 = **덮어써진 기존본** (`refreshPatchPreview`) — 비교 기준이 손대기 전 기존본이라
      파란 셀이 곧 이번 작업의 변경분
- [x] 결과 화면 `patchResultStage` — 값 반영/새로 추가/바뀐 셀/짝 못 찾은 행 통계,
      바뀐 셀 파란 굵은 글씨 · 새 행 초록 배경, 변경 보고서 CSV
- [x] `Exporter.writePatch` — 기존 헤더 구성 그대로 내보내기 → 그 파일을 다시 불러와 다음 컬럼 이어서 작업
- [x] 세션 저장/복원에 `base`·`focusColumns`·`columnMode` 반영 (옵셔널 — 이전 세션도 그대로 열림)
- [x] **CSVParser CRLF 버그 수정** — Swift에서 `"\r\n"`은 한 개의 Character(grapheme cluster)라
      상태 기계가 줄바꿈으로 못 알아보고 파일 전체를 한 줄로 읽었다. 이 도구가 내보내는 CSV가
      CRLF라 ‘내보낸 파일을 다시 불러오기’가 전부 깨져 있던 문제 (참조 파일 불러오기 포함).
      → 파싱 전에 CRLF/CR을 LF로 통일. 따옴표 안 줄바꿈·이스케이프 따옴표 회귀 테스트 통과.
- 검증: PatchEngine 35개 시나리오 + CSV 파서 10개 회귀 테스트 통과, `xcodebuild` BUILD SUCCEEDED

## 갈림길 화면 쉽게 고치기 (“내가 뭘 하고 싶은지 감이 안 온다”)
- [x] 제목을 추상적 질문 → 구체적 질문으로: `이번 작업을 어떻게 할까요?` → **`결과물을 어디에 만들까요?`**
- [x] 카드 이름을 동작 그대로: `만들던 파일 이어서 고치기` / `컬럼 구성만 따라 하기` / `처음부터 새로 만들기`
- [x] 카드마다 **“이럴 때”** 한 문장을 사용자 목소리로 (예: “지난주에 도시까지 정리해서 저장해 뒀어요…”)
- [x] 카드마다 **실제 값이 보이는 예시 상자** (`exampleLine` — `도시 서울 → Seoul`, 안 바뀌는 줄은 `=` 로)
- [x] 카드마다 **결과물 한 줄** (`결과물 = 그때 저장한 파일 그대로 + 오늘 고른 컬럼만 바뀜`)
- [x] 가로 3단 → **세로 3줄** 배치 (좁은 창에서 예시가 잘리지 않게)
- [x] 하단에 **`어떤 걸 골라야 할지 모르겠다면`** 안내 — 질문 하나(저장해 둔 결과 파일이 있나요?)로 셋 중 하나에 도달

## 예시로 규칙 만들기 (지금 값 → 바꾸고 싶은 값)
정규식을 몰라도, 고치고 싶은 예를 두어 개 적으면 같은 규칙이 걸리는 나머지 값까지 정리.
- [x] `RuleInference.swift` — 예시에서 규칙을 역추론 (`RuleExample` / `RuleCandidate`)
      - 후보를 넉넉히 만들고 **사용자 예시를 하나도 빠짐없이 재현하는 것만** 남김
      - 통째 규칙: 앞뒤 공백 / 공백 합치기·없애기 / 대소문자 / 첫 글자 대문자 / 숫자만 / 괄호 묶음 제거
      - 차이에서: `‘특별시’ 없애기` · `‘A’를 ‘B’로` · 앞뒤 떼기/붙이기
      - 글자 단위: `‘-’ 모두 없애기` · `‘.’ 모두 ‘-’로` (하이픈 두 개처럼 흩어진 변경은 diff로 못 잡음)
      - 구분자: `‘ (’ 앞부분만` / `마지막 ‘/’ 뒷부분만`
      - 길이: `앞 2글자만` / `뒤 4글자만`
      - 숫자 재묶기: `01012345678 → 010-1234-5678` (자릿수 다르면 손대지 않음)
      - 조각 순서 바꾸기: `31/05/2004 → 2004-05-31` (정규식 캡처 그룹 자동 생성)
      - 마지막 보루: `예시로 적은 N개 값만 그대로 바꾸기` (다른 값 영향 없음)
- [x] 동작이 완전히 같은 후보는 하나만 (컬럼 실제 값 80개로 비교) · 사용자가 보여 준 규칙이 일반 변환보다 위
- [x] **어떤 규칙도 값을 빈칸으로 만들지 못함** (`RuleCandidate.apply` 마지막 방어선) · 빈 `바꾸고 싶은 값` 예시 거부
- [x] `ExampleRuleSheet.swift` — 3단계가 한 화면에 (①예시 적기 ②규칙 고르기 ③바뀔 값 미리보기)
      - `지금 값`은 컬럼 실제 값 목록에서 고를 수 있음 (첫 줄은 최빈값으로 미리 채움)
      - 예시 줄 몇 개든 추가/삭제, 규칙마다 `N종 바뀜 / 전체 M종 중` 영향 표시
      - 이 컬럼에 없는 값을 적으면 오타 경고
- [x] 검토 카드 액션 줄 맨 앞에 **`예시로 고치기`** (강조 버튼) — 가장 쉬운 길이라 첫 자리
- 검증: 규칙 추론 12개 시나리오 통과 (`쉬운 예제`부터 날짜 순서 뒤집기까지), `xcodebuild` BUILD SUCCEEDED

## 포지셔닝 전환: ‘전체 문서 생성기’ → ‘작업 중 쓰는 유틸’
한 번에 완성본을 만드는 도구가 아니라, 지금 필요한 컬럼만 고쳐서 돌려주는 도구로.

### 1. 스키마 고정 해제 — 아무 CSV/XLSX나
- [x] `UnifiedColumn` **enum(73컬럼 고정) → 문자열 기반 struct** — 헤더가 곧 컬럼
      - `init?(rawValue:)` 유지(빈 이름만 nil) → 기존 `compactMap`/`col()` 코드 그대로 동작
      - 이름 상수(`.code` `.email` …) 전부 보존 → 아카데미 전용 로직 무손상
      - 73컬럼은 `UnifiedColumn.academyPreset` (순서 프리셋) + `academyDerived` 로만 남음
- [x] `UnifiedColumn.allCases` 사용처 11곳 제거 → 이번 파일들에서 계산하는 `allColumns` /
      `ColumnReviewBuilder.universe(in:)` / `columnCandidates` 로 대체
- [x] `Exporter.makeCSV(columns:)` 를 필수 인자로 (‘전체 스키마’ 기본값이 더는 없음)
- [x] `BaseSheet` 단순화 — ‘스키마 밖 컬럼’ 개념 삭제, 모든 헤더가 동등한 컬럼
- [x] `applyReference` — 헤더를 그대로 컬럼 구성으로 (못 알아본 헤더 개념 없음)

### 2. 첫 화면 = 합칠 파일 다 올리기 + 고칠 컬럼 고르기
- [x] `Stage.work` 신설 + **기본 진입** (갈림길·4단계 없이 바로)
- [x] `workDropView` — “합칠 파일들을 모두 올려주세요”, **여러 개 동시 드롭/선택**
      (`acceptDroppedFiles` 는 이름순 정렬해 결과가 매번 같게)
- [x] `workFileStrip` — 올린 파일마다 N행·M컬럼 표시 + 빼기, ‘파일 추가’
- [x] `workPickRow` — 컬럼 체크 목록, ‘손볼 거리 있는 것만’ 이 기본 선택,
      한 파일에만 있는 컬럼은 `2/3 파일` 뱃지로 표시
- [x] `BaseSheet.stacked` — 올린 파일들을 **같은 이름 컬럼끼리 맞춰 이어 붙인 시트**
      (헤더 합집합, 없는 컬럼은 빈칸) = ‘아직 아무것도 안 고친 결과물’
- [x] `ValueApplier.run(plans:)` — 올린 순서 그대로 이어 붙이며 값 통일만 적용
- [x] 결과물 = 이어 붙인 시트에 고른 컬럼만 덮어쓴 것 → 기존 `PatchResult` 화면·내보내기 재사용
- [x] `PlanBuilder.passthrough(url:)` — 헤더가 곧 컬럼, 값 변형 없는 계획 (`FilePlan.passthrough`)
- [x] `ColumnReviewBuilder.plainColumns/plainReviews` — 파일 헤더 순서, 파생 컬럼 없음
      (`genericReview` 추출해 통합 흐름과 공유)
- [x] `ValueApplier.run` — 병합·중복·파생 없이 값 통일만 적용 (Code 없는 파일도 안전)
- [x] `RowMatch` 3종 —
      `.position` 올린 파일을 그대로 합칠 때 (행 순서, 키 컬럼이 없어도 안전) ·
      `.key` Code→전화→이메일 ·
      `.column(_)` **사용자가 고른 컬럼**(사번·주문번호·학번 등)
- [x] ‘만들던 통합본에 이어붙이기’를 첫 화면 버튼으로 + `짝짓기 기준` Picker
      (`autoMatchColumn` — Code·전화·이메일이 있으면 자동, 없으면 값이 행마다 고유한 컬럼)
- [x] 결과·내보내기 문구를 유틸답게 (`고친 값 가져가기` / `정리된 파일 저장…` / `_정리.csv`)
- [x] 아카데미 통합 흐름은 `여러 파일을 하나로 합쳐야 해요` 링크로 계속 접근 가능 (동작 무변경)

### 3. 게이트 제거 — 언제든 가져가기
- [x] `canMerge` 를 항상 true 로. 미해결이 남아도 막지 않고 몇 종 남았는지만 알림
- [x] 문구 교체: ‘결정할 일 N건 남음(막힘)’ → ‘아직 정리 안 한 값 N종 (그대로 둬도 됩니다)’

- 검증: 여러 파일 합치기 27건(컬럼 이름이 다른 3파일 → 합본 → 다음 달 이어붙이기) +
  단일 파일 유틸 28건 + 아카데미 회귀 16건 + 이어붙이기 35건 + CSV 파서 10건 +
  규칙 추론 12건 통과 · `xcodebuild` BUILD SUCCEEDED

## 시작 화면 정리 + 한 번에 한 컬럼씩
목록이 8~9개씩 주욱 늘어져 뭘 해야 할지 안 보이던 문제.

### 1. 맨 처음에 ‘틀 파일’도 함께 묻기
- [x] `workBaseInvite` — 파일 드롭 화면 아래에 “합칠 모양(틀)이 되는 파일이 있나요?” + `틀 파일 고르기…`
- [x] 고르면 초록 칩(`틀: 파일명` · 해제)으로 바뀜 — 그 파일의 컬럼 구성·값은 그대로 두고 고른 컬럼만 덮어씀
- [x] `chooseBase`에 `plans.isEmpty` 분기 추가 — 파일보다 틀을 먼저 골라도 첫 화면에 머무름
      (기존엔 컬럼이 없는 `.focus` 화면으로 빠졌음)

### 2. 이미 정리된 컬럼은 접어 두기
- [x] `workColumnSplit` — 손볼 거리 있는 컬럼 / 정리된 컬럼으로 한 번만 가름 (focusStatus 재계산 방지)
- [x] `workColumnList` — 손볼 컬럼만 펼쳐 두고, `정리된 컬럼도 보기 (N개)` 토글로만 나머지 노출
      (`showSettledColumns`, 접힌 채 선택된 게 있으면 `N개 선택됨` 뱃지)
- [x] `손볼 거리 있는 것만` → 접기 / `전체 선택` → 펼치기 (고른 걸 숨기지 않음)
- [x] 손볼 거리가 하나도 없으면 `workAllSettledCard` 안내 (빈 목록 대신)

### 3. 검토는 한 번에 한 컬럼씩
- [x] `stepOrder`/`stepIndex` — 검토 진입 시 결정 순서를 한 번 고정 (고치는 동안 카드가 튀지 않게)
- [x] `결정 TODO` 탭이 목록 → **한 장 카드**로: `stepHeader`(N/M번째 · 해결 진행 막대 · 컬럼 목록 점프 메뉴)
      + 늘 펼쳐진 카드(`ReviewSection.alwaysExpanded`) + `stepFooter`(← 이전 / 나중에 보기 / 다음 →)
- [x] 남은 값이 있으면 `그대로 두고 다음 →` (‘이대로 확정’ 체크하며 이동), 없으면 `다음 →`
- [x] 마지막 다음 → `stepDoneCard` — 처음부터 다시 보기 / 안 끝난 컬럼으로 가기 / 가져가기
- [x] `syncSteps` — 결정 목록 구성이 바뀔 때만 순서 재계산, 보던 컬럼은 그대로 이어 봄
- [x] `전체 컬럼` 탭은 기존 목록 그대로 (한눈에 훑고 싶을 때)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### 1-b. 틀 파일을 넣었는데 바로 다음 화면으로 넘어가던 문제
- 원인: 첫 화면 전체가 하나의 드롭 영역(`acceptDroppedFiles`)이라, 틀로 넣은 파일도
  `addWorkFiles` → `adoptWorkPlans` 로 들어가 ‘합칠 파일’이 되고 컬럼 고르기 화면으로 넘어갔음
- [x] 첫 화면을 두 칸으로 분리: `① 합칠 파일들을 모두 올려주세요` / `② 맞출 틀이 있나요? (선택)`
- [x] ② 칸에 자체 `.onDrop`(`acceptBaseDrop`) — 여기 떨어뜨린 파일은 합쳐지지 않고 틀로만 쓰임
      (안쪽 드롭 영역이 바깥보다 먼저 받음), `isBaseDropTargeted` 강조
- [x] `chooseBase`를 패널 + `loadBase(url:)` 로 분리해 드롭·버튼이 같은 경로를 쓰게
- [x] 틀이 잡히면 초록 칸으로 바뀌며 `이제 ①에 합칠 파일을 올려 주세요` 안내 + `바꾸기…`/`해제`
- [x] ① 칸 아래 `전에 만들어 둔 통합본은 여기 말고 아래 ②에 넣어 주세요.` 한 줄

## 검토 화면: 세그먼트 탭 → 목록 + 상세 페이지
`결정 TODO / 전체 컬럼` 세그먼트가 헷갈린다는 피드백. 탭을 없애고 “전체 컬럼 목록 →
하나 고르면 상세 페이지” 한 갈래로.
- [x] `ReviewTab` enum·`reviewTab` 상태·`reviewTabBar` 세그먼트 제거
- [x] `columnListPage` — 전체 컬럼 한 목록 (`orderedReviewColumns`: 손볼 거리 ⚠️ 있는 컬럼이 위,
      그다음 원래 컬럼 순서). 순서는 검토 진입 시 한 번 고정(`stepOrder`)해 고치는 동안 줄이 안 튐
- [x] `columnRow` — 상태 아이콘 + 컬럼명 + `subtitle(for:)` 요약 + `N종 남음` 뱃지 + `›`
      · 추천값이 있으면 줄에서 바로 `추천값으로 N건` (상세로 안 들어가도 해결 가능)
- [x] `columnListHeader` — 컬럼 N개 · `해결 x / y` 진행 막대 · `손볼 컬럼부터 시작하기 — <컬럼명>` 버튼
- [x] `columnDetailPage` — 컬럼 하나만 있는 상세 페이지 (`openColumn`이 nil이면 목록)
      · `detailNavBar` : `‹ 컬럼 목록` · 컬럼명 · `3 / 12` · 이전/다음 컬럼
      · `detailIntro` : **‘이 컬럼은 이런 값이에요’** — 한 문장 설명(`explain`) + 행/종/손볼 값 칩
        + 지금 들어 있는 값 샘플 칩(`sampleValues`)
      · 본문은 기존 검토 카드(`ReviewSection`, 항상 펼침)
      · `detailFooter` : ← 이전 컬럼 / 목록으로 / `그대로 두고 다음 →`(남은 값은 ‘이대로 확정’)
- [x] 안 쓰게 된 `progressHeader`·스텝 위저드(`stepHeader`/`stepFooter`/`stepDoneCard`) 제거
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 판정 기준을 ‘완성될 틀’로 (접기·손볼 거리)
접기 기준이 값 자체의 오타·형식(`openCount`)만 봐서, 틀에 비추면 손볼 게 있는 컬럼도
정리된 것으로 접히던 문제. (예: 틀의 도시는 `Seoul/Busan`인데 이번 데이터는 `서울`)
- [x] `BaseSheet.distinctValues(_:)` / `looksCategorical(_:values:)` — 틀이 그 컬럼에 가진 값 목록,
      그리고 그 목록을 정답지로 써도 되는 컬럼인지(값 종수 ≤40 · 종수×3 ≤ 채워진 행 · 평균 길이 ≤40)
      → 이름·이메일처럼 행마다 다른 컬럼엔 목록을 들이대지 않는다
- [x] `indexBase()` — 틀을 잡을 때 한 번만 훑어 `baseValues`/`baseCategorical` 색인
- [x] `TemplateGap` + `templateGap(_:)` — 틀 기준 진단
      · `isNew` 틀에 없는 컬럼(결과 맨 뒤에 새로 생김) · `outside` 틀 목록 밖 값 종 수
      · `locked` 이미 틀 목록으로 잠금(그때부턴 `openCount`가 셈) · `comparable` 범주형 여부
- [x] `focusStatus` 가 틀 어긋남까지 반영 → 그런 컬럼은 **접히지 않고** 위에 남음
      뱃지도 상황별로 (`손볼 거리 있음` / `틀에 없는 컬럼` / `틀과 다름`)
- [x] 검토 목록(`columnRow`)·정렬(`orderedReviewColumns`)에도 같은 신호 반영
- [x] 컬럼 상세에 `templateNote` — “틀에는 이 컬럼에 12종이 들어 있어요. 이번 값 중 7종이 그
      목록에 없습니다” + **`틀의 값 목록으로 맞추기`** 버튼(`lockToBase` → 허용 목록 잠금 →
      기존 유사도 추천·선택 메뉴가 그대로 작동) / `틀 목록 잠금 해제`
- [x] 첫 화면 툴바에 틀 커버리지 한 줄 (`baseCoverageLine`) —
      `틀 ‘2분기.csv’ 컬럼 24개 중 18개를 이번 파일이 채웁니다 · 6개는 기존 값 그대로 · 새 컬럼 2개`
- [x] 세션 저장/복원에 `baseIsUserFile`·`matchColumn` 추가 (옵셔널) — 이어서 하기로 열어도
      틀이 틀로 복원됨 (이전엔 올린 파일을 이어 붙인 시트로 오인)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 이름이 다른 같은 컬럼 찾기 (틀의 값 ↔ 이번 값 패턴 매칭)
`거주지` ↔ 틀의 `도시`처럼 이름만 다른 컬럼을, 값을 견줘 짝지어 준다.
- [x] `ColumnMatcher.swift` — 값으로 컬럼을 알아보는 도구
      · `shape(_:)` 값의 모양 — 숫자 `9`, 로마자 `a`, 한글 `가` + 자릿수 (`010-1234-5678` → `93-94-94`)
      · `coarseShape(_:)` 길이를 뺀 모양 (`a@a.a`) — 이메일처럼 길이가 들쭉날쭉한 값용
      · `Profile` — 정규화 값 집합 + 모양 히스토그램(fine/coarse) + 샘플
      · 점수 = **값 겹침**(0.9·겹침+0.1·이름) / **모양 겹침**(0.75·모양+0.25·이름) /
        **이름 유사도**(0.85·이름+0.15·모양) 중 가장 높은 것, 0.5 미만이면 제안 안 함
      · 근거 문구를 함께 (`값이 3종 겹침` / `값 모양이 같음 (010-1111-2222)` / `이름이 비슷함`)
- [x] `ColumnMatchSheet` — 제안 확인 화면. 줄마다 `이번 컬럼 → [틀 컬럼 ▾]` +
      양쪽 실제 값 나란히. 기본 선택은 60% 이상이고 1·2위 차가 벌어질 때만,
      모호하면 `비슷한 후보가 여럿이에요` 경고만 띄우고 **아무것도 고르지 않음**
- [x] 첫 화면 배너 — `이름이 다른 같은 컬럼이 있어 보여요 (2개) · 거주지 → 도시 (90%)` + `짝지어 주기…`
- [x] `refreshMatches()` — 틀에 없는 이번 컬럼 ↔ 이번 파일이 안 채우는 틀 컬럼만 후보로,
      파일·틀이 바뀔 때만 계산 (`adoptWorkPlans`/`loadBase`/`restore`/적용 후)
- [x] `applyMatches(_:)` — 고른 짝만 실제로 이동: 행의 열쇠(헤더 이름)를 틀 이름으로 바꾸고
      `sources`/`separators`를 옮김. **값은 한 글자도 안 바뀜**, 이미 해 둔 값 통일·타입 설정도
      새 이름으로 따라감. 그 컬럼을 이미 가진 파일은 건드리지 않음(두 컬럼이 한 칸에 섞이지 않게)
- 검증: 매칭 시나리오 6건 통과 (값 겹침 도시 90% · 전화 모양 75% · 이메일 53% ·
  자릿수 다른 숫자 컬럼 구분 · 엉뚱한 짝 제안 안 함 · 모호한 경우 1·2위 차 확인),
  `xcodebuild` BUILD SUCCEEDED

### 크래시 수정 — `ColumnMatcher.shape` 에서 앱이 죽던 문제
- 원인: `out.append(runLen <= 12 ? Character("\(runLen)") : "n")` — runLen이 10~12면
  `Character("10")`이 두 글자라 런타임 트랩. 영문 11자 단어(`Information`)나
  11자리 전화번호(`01012345678`)만 있어도 죽었다. (테스트 값이 다 짧아서 안 걸렸음)
- [x] `String(runLen)`으로 교체 — 숫자는 늘 종류 문자 뒤에만 오므로 여러 자리라도 안 헷갈림
- [x] 회귀 테스트 추가: 긴 값(`Information` → `a11`, 한글 14자 → `가n`)에서 통과
- 확인: 매칭 시나리오 7건 + `xcodebuild` BUILD SUCCEEDED

## 첫 화면 컬럼 고르기 — 목록 대신 하나씩 제안
컬럼이 8~12개씩 늘어져 있으면 뭘 골라야 할지 모르겠다는 피드백.
- [x] `proposalOrder` — 손볼 거리가 있는 컬럼만, **일이 적은 순서**로
      (`openCount + 틀에 없는 값 + 새 컬럼`) — 금방 끝나는 것부터
- [x] `workProposalCard` — “이 컬럼부터 할까요?” 카드 한 장
      · 컬럼명 + `가장 빨리 끝나요` 뱃지(1순위) + 상태 문구 + 실제 값 샘플 칩
      · `이것부터 고치기 →`(이 컬럼만 골라 바로 검토) · `이번에 같이 고치기`(누적) · `다음 제안 →`
      · `손볼 컬럼 5개 · 제안 2번째` 로 전체 규모도 같이 보여 줌
- [x] 전체 체크 목록은 `전체 컬럼 목록에서 직접 고르기 (12개)`로 **접어 둠**(`showAllColumns`),
      빠른 선택 버튼(손볼 것만/전체 선택/해제)도 목록을 펼쳤을 때만 노출
- [x] 기본 선택을 ‘손볼 컬럼 전부’ → **아무것도 안 고름**으로 (제안에서 하나씩 고르는 흐름)
- [x] 툴바 버튼 문구에 고른 개수 (`고른 3개 고치러 가기 →`)
- [x] 컬럼 하나만 골라 들어가면 검토 목록을 건너뛰고 **바로 그 컬럼 상세**로 (`startWork`)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### ‘미리 완성되는 컬럼’ 보장
하나씩 제안하는 흐름으로 바뀌면서 고르지 않은 컬럼이 빠지는 것처럼 보이던 문제.
- [x] `settledColumns` — 손댈 게 없어 안 골라도 결과에 그대로 들어가는 컬럼
- [x] `workSettledSummary` 카드 — `손댈 것 없이 완성되는 컬럼 7개` + 이름 나열(모두 보기) +
      `고르지 않아도 결과 파일에 그대로 들어갑니다` 안내
- [x] `autoEditableColumns` — 그중 이미 정해 둔 통일 규칙이 있어 **판단 없이 다듬을 수 있는** 컬럼
      → 체크박스(기본 켬)로 `focusOrdered`에 포함시켜 결과에 반영
- [x] 고른 컬럼이 없어도 막지 않음 — 툴바 버튼이 `손 안 대고 그대로 가져가기 →`로 바뀌어 바로 내보내기
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 두 가지 일을 화면에서 갈라 놓기 (① 합치기 / ② 정리)
이 앱이 하는 일은 둘이다: **파일들을 하나로 합치기**(컬럼이 제자리에 들어가는가)와
**합쳐진 컬럼의 포맷·클렌징**. 지금까지 제안 순서가 ②만 보고 있었다.
- [x] `workMergeCard` — ① 합치기 점검 카드
      · `파일 3개를 세로로 쌓아 336행 · 컬럼 14개`
      · 충돌 없으면 초록 `충돌 없음` + “합치기는 끝났고 남은 일은 값 정리뿐”
      · `공통 컬럼 9개는 그대로 겹쳐집니다`
      · `이름이 다른 같은 컬럼 2건` + `짝지어 주기…`
      · `한 파일에만 있는 컬럼 3개 — 없는 파일의 행은 빈칸`
      · `파일마다 값 모양이 다른 컬럼 1개` + `‘생년월일’ 정리하기`(바로 ②로)
- [x] 제안 카드 제목에 `② 정리 —` 표시
- [x] **틀 없이 파일들끼리도 컬럼 짝짓기** (`fileToFileSuggestions`) —
      모든 파일에 있는 컬럼은 이미 짝이 맞으니 **일부 파일에만 있는 컬럼끼리만** 견주고,
      서로 다른 파일의 것끼리만 짝지어 한 파일 안에서 두 컬럼이 겹치지 않게.
      남는 이름은 먼저 나온(앞 파일의) 이름
- [x] `computeShapeConflicts()` — 같은 이름인데 파일마다 값 모양이 크게 다른 컬럼(모양 겹침 < 0.4)
- [x] `ColumnMatchSheet` 문구를 틀 있을 때/없을 때 모두 자연스럽게
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 완성본 미리보기 (파일별 색 · 쪼개진 컬럼 표시)
- [x] `workPreviewCard` — 첫 화면에 `지금 합치면 이렇게 나옵니다` 표
      · 파일마다 앞 몇 줄씩 골고루(`previewSampleRows`), 줄 배경·좌측 막대가 **파일 색**
      · `filePalette`(파랑·초록·주황·보라·분홍·청록) — 파일 칩(`workFileStrip`)도 같은 색
      · 아직 한 칸으로 안 합쳐진 컬럼(`isSplitColumn`)은 머리글 주황 + `2/3 파일`,
        짝 후보가 있으면 `↔ 도시?`(`pairHint`), 열 전체에 옅은 주황
      · 그 파일에 없는 값은 `—`, 범례 바(파일 색 · 쪼개진 컬럼 · 빈칸)
      · `큰 창으로 보기…`로 기존 미리보기 창 열기
- [x] 제안 카드도 값 칩 → **합쳐진 표 조각**으로 (`proposalPreview`)
      · `파일 | Code/Korean Name | 그 컬럼` 5줄, 파일 색 그대로
      · 손볼 값이 있는 줄을 먼저 골라 주황 굵은 글씨 + ⚠️ (`openValues`)
      · `합쳐진 표에서는 이렇게 보여요` 안내
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 미리보기 창에도 파일 색 · 기호 대신 숫자 · 제안 컬럼 포커싱
- [x] `BaseSheet.rowOrigins` — `stacked`가 만들 때 행마다 몇 번째 파일에서 왔는지 기록
      (세션 스냅샷에도 옵셔널로 저장 → 이어서 하기에도 색 유지)
- [x] `PreviewModel.rowFiles`/`fileNames` + `filePalette`(단일 출처) + `fileTint(row:)`/`fileLabel(row:)`
- [x] 큰 미리보기 창: 맨 앞에 **파일 칸**(색 막대 + 파일명), 행 전체에 옅은 파일 색,
      범례에 `파일 색` 줄 추가. 새로 붙은 행은 회색 `새 행`
- [x] `①②` 원문자 → `1. 합치기` / `2. 정리` / `위 1번 칸` 처럼 평범한 숫자 표기로 전부 교체
- [x] 제안 카드의 미니 표를 **이웃 컬럼과 함께** (`neighborColumns` — 앞뒤 2칸 + 식별 컬럼)
      · 대상 컬럼은 넓게 + 파란 배경 + 좌우 세로선 + `지금 볼 컬럼` 라벨
      · 카드 문구에 위치 안내 (`합쳐진 표의 5번째 컬럼 · 2/3 파일에 있음 (7월.csv · 8월.csv)`)
- [x] 완성본 미리보기에서도 **지금 제안 중인 컬럼**을 같은 파란 기둥으로 강조 (`currentProposalColumn`)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### 큰 미리보기 창이 흑백으로 나오던 문제
- 원인 1: 상태 색(주황/초록)을 `visibleReviews`(= 고른 컬럼)로만 계산 —
  첫 화면에서는 고른 컬럼이 없어(기본 선택 없음) 전부 무채색이 됐다
  → 두 미리보기 경로 모두 **전체 `reviews`** 기준으로 계산
- 원인 2: 파일 색·쪼개진 컬럼 표시가 창으로 전달되지 않음
  → `sendColumnMarks()` — `splitColumns`/`pairHints`를 `PreviewModel`로 넘기고,
    `cellTint`가 쪼개진 컬럼에 옅은 주황, 머리글에 `↔ 도시?` 뱃지, 범례에 항목 추가
  → 첫 화면에서 제안 중인 컬럼을 창에서도 파란 기둥으로 (`preview.focused`)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### 큰 미리보기 창: 번호 · 색 토글 · 버튼 위치
- [x] 머리글에 **열 번호**(`1. 2. 3.`) — 제안 카드의 `합쳐진 표의 5번째 컬럼`과 바로 대응
- [x] 맨 앞 칸에 **행 번호** + 파일 이름·색 (`rowHeadCell`)
- [x] 창 툴바에 `색 표시` 체크박스 (기본 켬) — 끄면 파일 색·상태 색 없이 흑백으로
- [x] 머리글 배경에도 쪼개진 컬럼 옅은 주황 추가, 컬럼 폭 150 → 170
- [x] `큰 창으로 보기` 버튼을 미리보기 카드 → **툴바의 ‘파일 더 넣기…’ 왼쪽**으로 이동
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### 이름 통일 + 큰 창을 인라인 미리보기와 같은 모습으로
- [x] 버튼·창 제목을 `완성본 미리보기`로 통일 (툴바 · 검토 화면 · 윈도우 타이틀)
- [x] 창 머리글을 인라인과 같은 **두 줄**로: `3. 도시`(쪼개진 컬럼이면 주황) +
      둘째 줄에 `2/3 파일` 또는 `↔ 거주지?`, 지금 보는 컬럼이면 `지금 볼 컬럼`
- [x] `sendColumnMarks`가 인라인과 **같은 문구**를 만들어 넘김
- [x] 빈 셀을 인라인처럼 `—`(옅은 회색)로, 범례에 `그 파일엔 없는 값` 추가
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### 완성본 미리보기 오표시 두 건 수정
- [x] **파생·빈 컬럼에 `0/3 파일` 쪼개짐 표시가 붙던 문제** — `isSplitColumn`이
      `owners < plans.count`만 봐서, 어느 파일에도 없는 컬럼(Code·지원방식·중복삭제 등
      자동 생성 컬럼, 73컬럼 스키마의 빈 자리)까지 주황으로 잡혔다
      → `owners > 0 && owners < plans.count` 로 수정
- [x] **아카데미 합치기 결과에 파일 색이 없던 문제** — 유틸/이어붙이기 경로에만 행 출처가 있었다
      → `MergeResult.origins` 추가 (ingest 때 파일 순번 기록, 중복 표시는 행을 지우지 않아
        순서·개수가 유지되므로 그대로 유효), `currentRows()`가 출처까지 반환,
        유틸 경로는 파일별 행 수로 계산 → 두 경로 모두 파일 색 표시
- [x] 머리글 둘째 줄(`2/3 파일`)이 잘리던 것 — 머리글 높이 34로 고정
- 참고: 창 툴바의 `색 표시`가 꺼져 있으면 의도적으로 흑백입니다 (기본은 켬)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### 출처를 사람 말과 색으로
- [x] `0/3 파일` 같은 표기 삭제 → `splitCaption` 이 사람 말로:
      · 짝 후보가 있으면 `‘거주지’와 같은 컬럼일까요?`
      · 한 파일에만 있으면 `‘7월.csv’에만 있음`
      · 두 파일에만 있으면 `‘9월.csv’엔 없음` (없는 파일 이름을 댄다)
      · 모든 파일에 있으면 `모든 파일에 있음`, 자동 생성·빈 컬럼은 아무 문구도 안 붙임
- [x] `ownerDots` — 파일 수만큼 점: 들어 있는 파일은 그 색, 없는 파일은 빈 동그라미(툴팁에 파일명)
- [x] **열 배경색 = 그 컬럼이 온 파일** (`columnOwnerTint`/`PreviewModel.ownerTint`) —
      한 파일에서만 온 컬럼은 머리글·셀이 그 파일 색. 상태색(주황/초록)이 있으면 그쪽이 우선
- [x] 인라인·큰 창 양쪽 동일하게, 범례에 `열 배경·점 = 그 컬럼이 들어 있는 파일` 추가
- [x] 타입 체크 시간 초과 → 머리글/셀을 `previewHeaderCell`·`previewBodyCell` 로 분리
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### 앱 시작할 때 미리보기 창이 저절로 뜨던 문제
- 원인: `Window` 씬은 앱을 켤 때(그리고 창 복원 시) 자동으로 열린다.
  macOS 15의 `defaultLaunchBehavior(.suppressed)`는 배포 타깃이 13.0이라 못 씀
  (`SceneBuilder`는 `if #available` 분기도 못 받는다)
- [x] `PreviewModel.openedByUser` — 사용자가 버튼으로 연 창인지 표시
- [x] `PreviewWindowView.onAppear`에서 그 표시가 없으면 스스로 `dismiss()`
- [x] 창을 여는 모든 곳을 `openPreviewWindow()` 하나로 모음 (표시 세우고 열기)
- 참고: 배포 타깃을 macOS 15로 올리면 `defaultLaunchBehavior(.suppressed)`로 더 깔끔해짐
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 완성본 미리보기 창에서 바로 작업하기
- [x] 머리글마다 **체크박스** — 고른 컬럼은 위쪽 파란 띠 + 셀 옅은 파란 배경
- [x] 고르면 툴바에 동작이 뜬다:
      · `데이터 정리하기` → 그 컬럼들로 `focusColumns`를 잡고 메인 창을 앞으로 가져와 값 검토 시작
      · `두 컬럼 합치기`(2개 골랐을 때) → 앞쪽 컬럼 이름을 남기고 한 칸으로
      · `선택 해제`, 아무것도 안 골랐을 땐 사용법 한 줄 안내
- [x] 창 → 메인 창 통신은 `PreviewModel.request`(`.clean` / `.merge`) + ContentView의 `onChange`
- [x] `applyMatches`에 **같은 파일에 두 컬럼이 다 있는 경우** 처리 추가 —
      기존엔 건너뛰었는데, 이제 `sources`를 이어 붙이고 구분자를 공백으로 (성+이름 같은 조합)
- [x] `색 표시`를 `@AppStorage`로 기억 (창을 다시 열어도 유지)
- [x] 창 툴바·셀을 `windowToolbar`/`summaryChips`/`viewOptions`/`bodyCell`로 분리
      (타입 체크 시간 초과 해소)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### 미리보기 색이 계속 안 나오던 원인 추적
- 데이터 경로는 이상 없음을 헤드리스로 확인 (3파일 → stacked → `rowOrigins [0,0,1,1,2]`,
  컬럼별 owners 정확, PatchEngine이 행 수를 유지 → 출처 인덱스 유효)
- [x] **이어서 하기(세션 복원) 때 색이 없던 진짜 이유** — 저장된 기준선에는 행 출처가
      없었다(이전 버전 스냅샷). 복원 시 사용자가 고른 틀이 아니면 `BaseSheet.stacked`로
      기준선을 다시 만들어 출처를 되살린다
- [x] `색 표시` 저장 키를 새로 (`previewShowColors.v2`) — 예전에 꺼 둔 값이 남아 흑백으로
      보이던 경우 방지, 기본 켬
- [x] 색을 진하게: 행 배경 0.08 → 0.14, 열 머리글 출처색 0.16 → 0.22, 셀 0.07 → 0.10
- [x] 그래도 못 그리면 이유를 표시: 툴바에 `행 출처 없음 — 파일 색을 못 그려요`
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

### 큰 창에 파일 색이 안 나오던 진짜 원인 (실행해서 확인)
빌드한 앱을 직접 띄워 창을 캡처해 확인했다. 창에는 새로 넣어 둔 진단 문구
`행 출처 없음 — 파일 색을 못 그려요` 가 떠 있었다 → `PreviewModel.rowFiles` 가 비어 있었음.
- 원인: **사용자가 고른 틀(`제출자(336명).csv`)에 이어붙이는 중**이라 미리보기의 행이
  올린 파일이 아니라 ‘틀의 행 + 새로 붙인 행’이었다. 틀에는 행 출처가 없으니
  `rowOrigins`가 비고, 예전 보정(`!baseIsUserFile`)도 걸리지 않았다.
- [x] `PatchResult.sourceRows` — 출력 행마다 어느 새 데이터 행에서 왔는지 (-1 = 짝 없음)
      · 짝을 찾은 틀 행은 **그 값을 넣어 준 파일 색**으로
      · 새로 붙인 행은 그 행이 온 파일 색으로
      · 짝을 못 찾아 기존 값 그대로인 행은 회색 `틀 그대로` (`PreviewModel.baseName`)
- [x] `refreshPatchPreview`가 새 데이터의 파일 출처(`MergeResult.origins`)를 함께 받아 매핑
- [x] 앱 시작 시 미리보기 창이 뜨던 것도 이번에 실제로 확인·수정 —
      `dismiss()`만으로는 안 닫혀서 `AppDelegate.applicationDidFinishLaunching`에서
      제목으로 찾아 닫고, 창을 `isRestorable = false`로 표시 (두 번 연속 실행해 확인)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 첫 화면 ‘완성본 미리보기’ 카드가 실제 완성본을 보여주게
- 그동안 이 카드는 이름과 달리 **올린 파일의 앞 몇 줄을 그대로** 보여 줬다
  (값 정리 전, 틀도 반영 안 됨). 색과 표시 규칙만 좋았을 뿐 내용이 결과물이 아니었다.
- [x] 카드가 `PreviewModel`(= 실제 결과표)에서 그리도록 교체 —
      값 통일·틀 이어붙이기가 반영된 행, 개선된 셀은 파란 굵은 글씨
- [x] `previewSampleRows` — 결과표에서 **파일마다 골고루** 뽑아 9줄 (어느 파일도 안 빠지게)
- [x] 부제를 정직하게: `지금 상태로 만들어진 결과입니다 — 전체 N행 중 M줄`
- [x] 첫 화면에서도 결과가 준비되도록 `adoptWorkPlans`/`onAppear`/`focusColumns` 변경 시 갱신
- 확인: 앱을 띄워 창만 캡처해 확인 (파일 색·출처 점·`—` 빈칸·틀 표시 모두 정상),
  `xcodebuild ... build` → BUILD SUCCEEDED

## 행마다 ‘어디서 온 데이터인지’ 밝히기
- [x] `틀 그대로` 라는 뭉뚱그린 표기 → **틀 파일 이름 그대로** 표시 (`제출자(336명).csv`)
- [x] 줄 머리에 짧은 꼬리표 (`PreviewModel.rowBadge`) — `신규`(이번에 붙인 사람) / `틀`(원래 있던 행)
- [x] `rowOriginHelp` — 마우스를 올리면 한 문장으로:
      · `12행 · 틀 ‘제출자(336명).csv’의 행 — ‘8월.csv’의 값으로 채웠습니다.`
      · `340행 · ‘8월.csv’에서 새로 붙인 행`
      · `77행 · 틀 ‘…’의 행 — 이번 데이터에 짝이 없어 그대로 뒀습니다.`
- [x] `PatchResult.newRowIndices`를 미리보기로 넘겨 신규/기존을 가름
- [x] 머리글 `파일` → `행 · 출처`, 범례에 회색 `틀: <파일명>` 추가 (인라인·큰 창 모두)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 틀에서는 ‘컬럼 이름만’ 가져오기
첫 화면의 틀 칸이 값까지 가져와(그 파일의 행 + 이어붙이기) 결과가 헷갈렸다.
틀은 **결과물의 컬럼 구성**만 정해야 한다.
- [x] `BaseSheet.stacked(_:name:template:)` — 틀 컬럼 이름·순서를 먼저 깔고,
      파일에만 있는 컬럼을 뒤에 붙인다. 틀의 **행은 한 줄도 안 들어온다**
- [x] `loadTemplateColumns(_:)` / `chooseTemplateColumns()` / `clearTemplateColumns()` —
      첫 화면 틀 칸(드롭·버튼)은 이제 컬럼 이름만 가져옴 (`templateName`/`templateColumns`)
- [x] 안내 문구 교체: `이 파일에서는 **컬럼 이름만** 가져옵니다 — 값은 올린 파일 것만 들어가요`
- [x] 요약 줄: `틀 ‘X’의 컬럼 24개 중 18개를 이번 파일이 채웁니다 · 6개는 빈칸 ·
      틀에 없는 컬럼 2개는 뒤에 붙습니다`
- [x] 파일을 더 올려도 틀의 컬럼 순서 유지 (`adoptWorkPlans`/`rebuildWorkColumns`)
- [x] 틀 기준 진단은 ‘틀에 없는 컬럼’ 표시로 (값 비교는 값을 가져올 때만)
- [x] 세션 저장/복원에 `templateName`·`templateColumns` 추가
- 참고: 값까지 이어붙이는 흐름은 컬럼 바의 `만들던 통합본에 이어붙이기…`로 그대로 남아 있음
- 검증: 헤드리스 테스트 통과 — 컬럼 순서가 틀을 따르고, 파일에만 있는 컬럼은 뒤에 붙고,
  틀의 값(홍길동·전화번호)은 결과에 한 칸도 들어오지 않음

## 틀을 넣으면 채울 수 있는 컬럼은 자동으로 채우기
틀에서 컬럼 이름만 가져오게 하니, 이름이 다른 원본(설문 문항 헤더)과 하나도 안 맞아
완성본이 전부 빈칸으로 보였다. “채울 수 있는 건 다 채워야” 한다.
- [x] 틀 파일의 값을 `templateValues`에 따로 보관 — **결과에는 안 들어가고**
      ‘이 컬럼이 저 컬럼이구나’를 가리는 데만 쓴다
- [x] `refreshMatches`에 틀(컬럼만) 갈래 추가 — 아직 아무 파일도 채우지 못한 틀 컬럼이 후보
- [x] `autoMatchTemplateColumns()` — 확실한 짝(70% 이상, 1·2위 차 0.08 이상)은 묻지 않고 바로 채움
      · 틀을 넣을 때, 그리고 파일을 더 올릴 때마다 실행
- [x] `되돌리기` — 자동으로 채우기 직전 `plans` 스냅샷을 두고 한 번에 원상복구 (`undoAutoMatch`)
- [x] 합치기 카드에 `자동으로 채운 컬럼 3개 — 성함 → Korean Name · …` + `되돌리기`
- 검증: 헤드리스 — 설문 문항 헤더 3개(`성함`/`현재 거주하고 있는 도시를…`/`연락 가능한 번호`)가
  틀의 `Korean Name`/`City`/`Phone Number` 로 모두 자동 연결됨
  (도시는 값 겹침 90%, 이름·전화는 값 모양 75%)

## 글씨 크기 기본을 .body 로
- [x] `.caption2` / `.caption` / `.footnote` / `.subheadline` / `.callout` 로 지정돼 있던
      글씨를 전부 **`.body`** 로 (ContentView 230곳 · ExampleRuleSheet 20 · ColumnMatcher 6 ·
      AppSettings 2 — 총 258곳). 제목(`.headline` / `.title*` / `.largeTitle`)은 그대로
- [x] 글씨가 커진 만큼 표 칸을 넓힘 — 미리보기 셀 132 → 152, 제안 카드 96 → 120 /
      110 → 130 / 170 → 190, 창 머리글 높이 34 → 42
- 확인: 앱을 띄워 창만 캡처해 확인 (파일 칩·합치기 카드·미리보기 표·범례 모두 정상),
  `xcodebuild ... build` → BUILD SUCCEEDED

## 1. 합치기도 한 번에 하나씩
확인할 일이 한꺼번에 붙어 있어 무엇부터 볼지 모르겠다는 피드백.
- [x] `MergeStep` — 합치기 단계에서 사람이 한 번씩 확인할 일 하나(제목·설명·동작)
- [x] `mergeSteps` — 지금 남은 할 일만, 순서대로:
      · `자동으로 채운 컬럼 N개` (무엇을 어떻게 이었는지 나열 + `되돌리기`)
      · `이름이 다른 같은 컬럼 N건` (+ `짝지어 주기…`)
      · `‘생년월일’는 파일마다 값 모양이 달라요` — **컬럼마다 한 장씩** (+ `정리하기`)
      · `한 파일에만 있는 컬럼 N개` (안내만)
- [x] 카드가 **맨 위 한 장만** 보여 주고, `이대로 둘게요`/`알겠어요`를 누르면 다음 것으로
      (`mergeDone` — 확인한 항목 기록, 파일 구성이 바뀌면 초기화)
- [x] 머리줄에 `확인할 일 3개 중 1번째`, 다 끝나면 `확인할 것 없음` 초록 + `확인한 것 다시 보기`
- [x] 공통 컬럼 수는 할 일이 아니므로 머리줄로 옮김 (`공통 컬럼 9개는 그대로 겹침`)
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 빈 컬럼에 ‘어떻게 채우는지’ 안내
틀에서 컬럼만 가져오면 값이 안 들어온 컬럼이 생기는데, 뭘 해야 채워지는지 알 수 없었다.
- [x] `emptyColumns` — 어떤 파일도 채우지 않거나 값이 전부 빈 컬럼
- [x] 합치기 스텝 `아직 비어 있는 컬럼 N개` — 컬럼 이름을 나열하고 방법 셋을 알려 준다:
      ① 짝이 될 만한 후보가 있으면 `‘전화번호’는 파일의 ‘연락 가능한 번호’일 수 있어요 (75%)`
         (없으면 “파일의 어느 칸이 이 컬럼인지 직접 골라 주기”)
      ② 그 값이 든 파일을 더 올리기
      ③ 원래 비워 두는 칸이면 그냥 두기 — 결과에도 빈칸
      · 버튼 `‘전화번호’ 채울 칸 고르기…` → 기존 `ColumnSourceSheet`(여러 칸 합치기)로 직접 지정
- [x] 그 시트를 닫으면 컬럼·값·미리보기를 다시 계산 (`rebuildWorkColumns`)
- [x] 컬럼 목록 줄에 `비어 있음` 뱃지 + 설명, 미리보기 머리글(인라인·큰 창)에
      `비어 있음 — 채울 칸을 골라 주세요` 주황 캡션
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 완성본 미리보기에서 값 고치기·복사
- [x] 셀 오른쪽 클릭 메뉴 (큰 창): `값 고치기…` / `이 값 복사` /
      `‘<컬럼>’ 열 전체 복사` / `표 전체 복사 (붙여넣기용)`
- [x] `값 고치기…` — 작은 창에서 `이전 값 → 새 값`, **같은 값이 있는 행 수를 미리 알려 줌**
      (`이 컬럼에서 ‘서울시’인 12행이 함께 바뀝니다`). 값 단위로 고치므로
      변경 보고서·미리보기 개선 표시에 그대로 남는다
- [x] `PreviewModel.PreviewRequest.edit` — 창에서 메인 창으로 전달, `editValue`가
      `valueMap`에 반영 (이미 그 값으로 통일해 둔 원본들도 함께 옮김)
- [x] 툴바에 `표 복사` — 지금 보이는 행을 탭 구분(행 번호·출처 포함)으로 복사 → 엑셀·시트에 바로 붙음
- [x] 첫 화면 인라인 카드에도 `이 값 복사` / `열 전체 복사` / `큰 창에서 고치기…`
- 확인: `xcodebuild ... build` → BUILD SUCCEEDED

## 올린 파일 원본 보기 + xlsx 머리글 줄 버그
- [x] 파일 칩을 누르면 그 파일 **원본 그대로** 보는 창 (`FilePreviewSheet`) —
      파일 색 막대 · `N행 · M컬럼 — 올린 파일 그대로입니다 (정리 전)` · 값 검색 ·
      표 복사 · 셀 오른쪽 클릭으로 값/열 복사 (앞 500행 표시)
- [x] **버그: 일반지원 xlsx가 Code만 채워지고 나머지가 다 비던 문제**
      - 원인: 그 파일은 **첫 줄이 파일 제목 한 칸**(`2027 패스트 트랙 1 - 일반지원 (111)`)이고
        진짜 머리글은 둘째 줄인데, `PlanBuilder.passthrough`가 언제나 첫 줄을 머리글로 읽었다
        (아카데미 흐름은 `headerRowIndex: 1`로 읽고 있어서 그쪽만 멀쩡했다)
      - [x] `XLSXReader.readTableAutoHeader` — 앞 5줄 중 **이름이 채워진 칸이 가장 많은 줄**을
        머리글로 (제목 줄 자동 건너뜀). `passthrough`·`BaseSheetLoader`·참조 파일 읽기에 모두 적용
      - 검증: 실제 파일 3개로 — 일반지원 407행·52컬럼 전부 값 있음, private 44행,
        Public 181행, 합쳐서 632행

## 파일 고르고 나서 멈춘 것처럼 보이던 문제
- 재현·계측: 실제 파일 3개(632행·113컬럼)로 재 보니 **읽기는 0.16초인데
  짝짓기 후보 계산이 3.27초** — 이게 메인 스레드에서 돌아 화면이 얼어 보였다
- [x] **짝짓기 계산 15배 빠르게** — `ColumnMatcher.suggest(sourceProfiles:targetProfiles:)` 추가.
      기존엔 소스 컬럼마다 모든 후보의 값을 다시 훑어(profile) O(n²)로 값 스캔이 일어났다.
      컬럼마다 한 번만 훑고 점수만 비교 → **3.27s → 0.21s**
- [x] 파일 읽기를 백그라운드로 옮기고 **진행 표시**(`loadingOverlay`) —
      `‘2027 패스트 트랙 1 - 일반지원 (111).xlsx’ 읽는 중… (1/3)` →
      `컬럼 맞추고 미리보기 만드는 중…`, 그 동안 `파일 고르기`·`파일 더 넣기` 잠금
- 계측(실제 파일): 읽기 0.16s · 이어 붙이기 0.02s · 컬럼 검토 0.10s · 짝짓기 0.21s

## 기다림 피드백 · 컬럼 폭 · 주황 글씨 · 키 컬럼
### 눌러서 기다려야 하면 그렇다고 말해 주기
- [x] `withBusy(_:_:)` — 안내를 먼저 그린 다음(한 프레임 뒤) 작업 시작.
      루트에 오버레이를 달아 **어느 화면에서 눌러도** 보이게
- [x] 적용: 완성본 미리보기 열기 · 검토 화면 만들기(제안 카드/툴바/합치기 스텝) ·
      컬럼 합치기 · 되돌리기 · 미리보기 창에서 온 정리/합치기/값 바꾸기 · 키 바꾸기

### 컬럼 폭 조절 (미리보기 창)
- [x] 머리글 오른쪽 끝 4px을 잡고 끌면 폭이 바뀜 (`widthHandle`, 90~700pt, 커서도 바뀜)
- [x] 머리글 오른쪽 클릭 → `이 컬럼 폭 기본으로` / `모든 컬럼 폭 기본으로`

### 주황 글씨 완화
- [x] 컬럼 이름·설명 글씨의 주황을 걷어내고 **기본색/회색**으로.
      신호는 출처 점(●○○), 옅은 배경, 작은 뱃지로만 (눈에 덜 아프게)

### 여러 파일을 키로 합치기
- [x] `BaseSheet.stacked(key:)` — 같은 키 값이면 파일이 달라도 **한 줄로 포갬**
      (먼저 들어온 값이 이기고 빈칸만 채움), 키가 비면 `AUTO-0001`처럼 **자동 생성**
- [x] `keyCandidates` / `autoKeyColumn()` — 절반 이상 채워지고 겹침 10% 이하인 컬럼 중
      Code·이메일·전화 우선으로 자동 선택
- [x] 합치기 첫 스텝이 키 확인: `키 컬럼: ‘Code’` + 무엇이 일어났는지
      (`632행 → 610행 (같은 키 22행 포갬)`, `키가 비어 있던 8행에 번호를 만들어 넣었어요`)
      + `키 바꾸기` 메뉴(후보들 · 키 없이 그냥 쌓기)
- [x] 키로 포개면 행 수가 달라지므로 값 반영도 `.column(key)` 로 짝지음
- [x] 세션 저장/복원에 `keyColumn` 추가
- 검증(실제 파일): 키 없이 632행 / Code 키 632행 · 만들어 준 키 225개(간편지원엔 Code가 없음)
  · 빈 키 없음 · 키 중복 없음

## 자동 부여되는 일련번호의 첫 값·규칙을 사람이 정하기
`6F10001부터 자동 부여` 처럼 규칙이 코드에 박혀 있어 바꿀 수 없었다.
- [x] `KeyPattern` — **규칙 대신 첫 값 하나**로 정한다 (`init?(example:)`)
      · `6F10001` → 6F1…0001, 다음은 `6F10002`
      · `A-001` → `A-002` · `1001` → `1002` · `2026-0007-KR` → `2026-0008-KR`
      · 숫자가 없으면 규칙으로 안 봄, `sample`로 `6F10001, 6F10002, 6F10003 …` 미리 보여 줌
- [x] `keyPatternEditor` — `첫 번호 [6F10001] → 6F10001, 6F10002, 6F10003 …`
      · 자동 생성 컬럼(Code) 검토 카드 안에, 그리고 합치기의 키 스텝에도 같은 칸
- [x] `BaseSheet.stacked(keyPattern:)` — 키가 빈 행에 그 규칙으로 번호를 붙이고,
      **원본에 이미 있는 번호는 건너뜀**
- [x] `MergeEngine.codePattern` — 아카데미 흐름의 `6F1%04d` 하드코딩 제거
- [x] 기본값: 유틸 흐름 `AUTO-0001`, 아카데미 흐름 `6F10001` · 세션에 저장
- 검증: 규칙 파싱 7건 + 실제 파일에 `6F10001…` 부여 확인

## 세로로 쌓을 때 새 줄을 되도록 만들지 않기 + 남은 일 표시
### 같은 사람이면 새 줄을 만들지 않는다
- [x] `BaseSheet.stacked(identity:)` — 키가 비어 있는 행도 **이메일·전화로 같은 사람을 찾아**
      기존 줄에 포갠다 (빈칸만 채움). 정말 처음 보는 사람일 때만 새 줄 + 새 번호
- [x] `BaseSheet.identityKey` — 이메일은 소문자, 전화는 숫자만 뒤 11자리로 비교
- [x] `identityColumns` — 값이 고유하고 이메일/전화처럼 생긴 컬럼을 자동으로 골라 씀
- 검증: 만든 예제(Code 있는 파일 + Code 없는 파일) 4행 → 3행, 기존 Code 유지, 새 사람만 새 번호
- 검증(실제 파일): 컬럼 짝짓기(`이메일 주소` → `Email Address`)를 적용하면
  **632행 → 562행 (70행 포갬)**. 짝짓기 전에는 컬럼 이름이 달라 못 알아보므로,
  합치기 단계에서 짝을 먼저 맞춰야 줄이 준다

### 남은 일 한 카드로
- [x] `workTodoSummary` — 제안 카드 아래에 세 줄로:
      · `채워야 할 컬럼 N개` (틀에는 있는데 값이 없는 컬럼) + `‘전화번호’ 채우기…`
      · `값을 정리할 컬럼 N개` (컬럼마다 `3종`·`파일마다 모양 다름`) + `‘도시’부터 정리 →`
      · `손댈 것 없이 완성되는 컬럼 N개` + 자동으로 다듬을 것 체크박스
      · 둘 다 없으면 초록 `채울 것도, 정리할 값도 없습니다`

## 컬럼 고르기 판 · 여러 컬럼 합치기 · 자동 저장
### 눌러서 고르는 컬럼 판
- [x] 접혀 있던 체크 목록 → **항상 보이는 칩 격자**(`workColumnBoard`, 적응형 2~4열)
      · 칩 = 이름 + 상태 뱃지 + 출처 점 + `240행 · 31종 · 미정리 3종`
      · 비어 있는 컬럼은 주황 점선 테두리
      · 빠른 선택 `손볼 것만` / `모두` / `해제`, 정리된 컬럼은 접었다 펴기
- [x] 고르면 위에 동작 줄이 뜬다 (`boardActionBar`)
      · 1개: `이 컬럼 정리하기 →` (비어 있으면 `채울 칸 고르기…`도)
      · 2개 이상: `N개 정리하기 →` · **`한 칸으로 합치기`**
- [x] 합치기 전 확인 시트 — `국문 성 · 국문 이름 → ‘국문 성’`,
      “앞 이름이 남고, 한 파일에 둘 다 값이 있으면 공백으로 이어 붙여요”

### 새 행을 만들지 않고 기존 행에 채우기 (짝짓기 없이도)
- [x] `BaseSheet.contactColumns(of:)` — **파일마다** 이메일·전화처럼 생긴 컬럼을 값 모양으로 찾아낸다
      (`이메일 주소`/`휴대폰 번호` ↔ `Email Address`/`Phone Number`를 미리 짝짓지 않아도 됨)
- [x] 키가 없어도(키 없이 쌓기) 같은 사람이면 기존 줄에 값을 채운다
- 검증(실제 파일, **짝짓기 전**): 632행 → **562행**(70행 포갬, Code 키) ·
  키 없이도 632행 → **549행**(83행 포갬)

### 작업 내용 자동 저장
- [x] `SessionAutosave` 모디파이어로 저장 감시를 한 곳에 모음 (본문 타입 체크 부담도 해소)
      · 값 통일·허용 목록·체크·선택·컬럼 구성·단계·파일 수·키·틀이 바뀌면 0.8초 뒤 저장
      · 창을 내리거나 앱을 벗어나면 즉시 저장
- [x] 툴바에 `자동 저장됨 · 3:24 PM` 표시 (툴팁: 앱을 껐다 켜도 이어서 하기로 돌아올 수 있음)

## 행은 딱 맞아야 한다 — 임의 병합 금지, 중복은 ‘표시’만
직전에 넣은 ‘같은 사람이면 한 줄로 포개기’를 **되돌렸다**. 데이터 정합성이 먼저다.
- [x] `BaseSheet.stacked`는 이제 **행을 하나도 지우거나 포개지 않는다** — 올린 파일 합계 = 결과 행 수
- [x] 대신 `duplicateRows`/`duplicateGroups`로 **중복으로 보이는 행만 표시** (같은 키·이메일·전화가
      앞줄에 이미 나온 행). 키가 비면 번호만 만들어 넣고 행은 그대로 둔다
- [x] `rowSourceIDs`(`파일#줄`) + `excluding:` — **사용자가 지우기로 한 행만** 빠진다
- [x] 합치기 스텝 `중복으로 보이는 행 83개` — “행은 하나도 지우지 않았습니다, 표시만 해 뒀어요”
      · `중복 행 83개 지우기` (누를 때만 지움) · `미리보기에서 중복만 보기`
- [x] 미리보기 창: 행 머리에 `중복` 뱃지, 툴바에 `중복만 (83)` 필터
- [x] 행 수 대조표: 파일별 행 수 → 합계 → 결과물,
      `행 수가 딱 맞습니다 — 빠진 행이 없어요` / `중복으로 보이는 행 83행 (표시만)` /
      `내가 지운 행 N개` + `되살리기`
- [x] 지운 행 목록은 세션에 저장 (되살리기 가능)
- 검증(실제 파일): 632행 → **632행** · 중복 표시 83행(81명) · 사용자가 지우면 549행,
  되살리면 632행

### Xcode에만 계속 뜨던 `Ambiguous use of 'opacity'`
- 원인: 컴파일은 되는데 **에디터(SourceKit)가 타입 추론을 제한 시간 안에 못 끝내고** 포기하면서
  엉뚱한 위치·문구로 오류를 띄우던 것. `-warn-long-function-bodies`로 재 보니
  `proposalPreview`가 **2457ms** (한도 800ms)
- [x] `proposalPreview`를 `proposalHeaderRow` / `proposalHeaderCell` / `proposalRow` /
      `proposalCell` 로 쪼개고, 색·폭에 타입을 명시 (`let border: Color = …`)
- [x] `workPreviewCard`도 `previewCardHeader` / `previewTable` / `previewCardBackground` 로 분리
- 확인: 느린 표현식 경고 **0건**, `xcodebuild` BUILD SUCCEEDED

### ‘행 · 출처’와 ‘새 행’이 무슨 말인지 모르겠다는 피드백
- [x] 머리글 문구를 풀어서: `행 · 출처` → **`행 · 어느 파일에서`**
      (미리보기 창은 `확정 · 행 · 어느 파일에서 왔나`)
- [x] 출처를 모르는 줄의 표시도 상황에 맞게:
      `이번에 추가`(틀에 없던 사람) / `틀에 없던 사람` / `출처 모름`

### ‘새 행’이 생기고 ‘출처 모름’이 뜨던 원인 두 가지
- [x] **키로 짝짓다가 새 행이 붙던 문제** — 우리가 만든 기준선(올린 파일을 그대로 쌓은 것)은
      새 데이터와 행이 1:1인데, 키(`Code`)로 짝짓게 해 놨더니 **같은 키를 가진 둘째 행이
      짝을 못 찾아 맨 아래에 새 행으로 붙었다.** → `!baseIsUserFile`이면 항상 `.position`
- [x] **‘출처 모름’** — 짝을 못 찾은 줄에 -1을 넣고 있었다.
      기준선이 이미 아는 출처(`rowOrigins`)를 그대로 쓰도록 수정
- 검증(실제 파일): 올린 합계 632행 · 기준선 632행 · 결과 **632행** ·
  새로 붙은 행 **0** · 짝 못 찾은 행 **0** · 출처 없는 행 **0** · 중복 표시 83행

## 중복 짝으로 이동 · 더블클릭으로 값 고치기
- [x] `BaseSheet.duplicateOf` — 중복 행이 **몇 행과 같은 사람인지** 기록
- [x] 미리보기 창의 중복 뱃지가 `중복 ↑12행` 버튼이 되고, 누르면 그 행으로 **스크롤 이동 +
      2초간 노란 강조** (`ScrollViewReader`, `jumpTarget`/`flashRow`).
      `중복만 보기`가 켜져 있으면 자동으로 풀고 이동
- [x] 셀을 **더블클릭**하면 값 고치기 창 (오른쪽 클릭 메뉴와 같은 동작)
- [x] 빈 칸도 더블클릭 가능 — `‘도시’ 빈 칸 채우기` / `이 컬럼에서 비어 있는 24행이
      모두 이 값으로 채워집니다`

## 무지개 커서(앱이 죽은 것처럼 보이는 것) 없애기
- [x] 미리보기 만들기를 **백그라운드로** 옮김 — `PreviewInput`/`PreviewPayload`/`PreviewBuilder`
      (BaseSheet.swift). 값 통일 적용 → 결과물 만들기 → 바뀐 셀 찾기까지 전부 순수 계산이라
      스레드를 가리지 않는다. 끝나면 메인에서 `PreviewModel.apply(_:)`로 한 번에 반영
- [x] `previewToken` — 늦게 끝난 옛 계산이 새 결과를 덮지 않게
- [x] 상태 의존 계산(컬럼 상태·색·뱃지)은 메인에서 먼저 → 색은 즉시, 행은 곧 따라온다
- [x] 미리보기 창에 `결과를 다시 만드는 중…` 표시 (`PreviewModel.isBuilding`)
- [x] 옛 `refreshPatchPreview`/`buildPatch`/`sendRowKeys`/`planRowOrigins`/`rowLabel` 제거
      (전부 `PreviewBuilder`로 옮겨감)

## 중복 표시·컬럼 이동 다듬기
- [x] 중복은 **표시만** — `12행과 중복` (누르는 버튼 아님)
- [x] `보는 중: 도시` 칩이 **그 컬럼으로 가로 스크롤**하는 버튼이 되고, 눈 모양 심볼 제거

## 틀 기준 미리보기 · 컬럼 표시 간소화 · 칸 합치기 설정
### 미리보기를 틀 기준으로
- [x] 컬럼 순서는 이미 틀을 따름(`stacked(template:)`). 여기에 **틀 밖 컬럼 표시**를 더함 —
      `PreviewModel.extraColumns`/`usingTemplate`, 머리글에 `틀 밖 컬럼` + 열 전체 옅은 회색
### 미리보기 컬럼 표시 간소화 (요청)
- [x] 컬럼이 어느 파일에서 왔는지 표시 제거 — 출처 점(●●○), `‘x.csv’에만 있음`, 출처 배경색
- [x] 머리글의 컬럼 번호(`1.` `2.`) 제거
### 여러 칸을 한 칸으로 (순서·구분자)
- [x] `ColumnMergeSetupSheet` — 어디에 넣을지(틀 안/밖 표시), **순서**(↑↓), **사이에 넣을 것**
      (붙여쓰기 / 공백 / 쉼표 / 하이픈 / 직접), 실제 값으로 `이렇게 됩니다` 미리보기
- [x] `applyColumnMerge(target:order:separator:)` — `sources`/`separators`만 바꾸므로 **행은 그대로**,
      원래 칸은 목록에서 빠지고 값 정리 규칙은 새 칸으로 옮겨 간다
- [x] 컬럼 판에서 2개 이상 고르면 `N개 정리하기 ▾` 메뉴 — `한 칸으로 합치기…` / `따로따로 정리하기 →`
- [x] 미리보기 창의 `두 컬럼 합치기`도 같은 설정 시트로 연결
- 검증: `국문 성` + `국문 이름` → `Korean Name` (`김 철수` / 붙여쓰기 `김철수` /
  순서 바꾸면 `철수 김`), 원래 칸은 컬럼 목록에서 빠짐

## “일반지원 파일이 왜 407행이지? 111행 아닌가”
- 확인: 파일은 실제로 **407행**이 맞고, 파일 이름의 `(111)`은 **제출 완료된 행 수**였다.
  `Process Status` 값 분포 — `Motivation & Portfolio 209` · **`Submitted 111`** ·
  `No application 26` · `Basic Data 26` · 빈 값 17 · `Education & Employment 12` · 그 외 6
  (아카데미 전용 흐름은 이 컬럼으로 걸러 111행만 쓰지만, 기본 흐름은 원본을 그대로 보여 준다)
- [x] **행 걸러 내기**를 기본 흐름에도 추가 — 임의로 지우지 않고 사람이 고른다
      · 합치기 스텝: `행을 걸러 낼까요? — ‘Process Status’로 나뉩니다` + 값별 행 수
      · 시트에서 남길 값 체크 (`Submitted`만 남기기 등), `남는 행 336행 / 전체 632행` 실시간
      · 기준 컬럼도 바꿀 수 있음 (값 종류 2~12개인 컬럼 자동 후보)
      · 빼는 행은 **지우는 게 아니라 감추는 것** — `되돌리기`/`거르지 않기`로 복구, 세션 저장
      · 행 수 대조표에 `‘Process Status’로 빼 둔 행 296행 [되돌리기]`
- 검증: 전체 632행 → `Submitted`만 남기면 **336행** (일반지원 111 + private 44 + Public 181)

## 버벅임 원인 분석과 해결
증상: 키 값을 고르거나 ‘첫 번호’를 타이핑하면 글자마다 앱이 멈칫.
- 원인: **화면을 다시 그릴 때마다 데이터를 통째로 훑고 있었다.**
  글자 하나 칠 때마다 `keyCandidates`(113컬럼 × 632행) · `identityColumns` ·
  `filterCandidates`+값 집계(113 × 632) · `focusStatus`(컬럼마다 `openCount`·`templateGap`) ·
  `workColumnSplit` · `emptyColumns` · `columnsNeedingClean` · `proposalOrder` 가 전부 다시 계산됐다
  (측정: 키 후보 0.04s + 필터 후보 0.04s — 여기에 컬럼별 상태 계산과 SwiftUI 재구성이 더해짐)
- [x] `WorkCache` 도입 — 위 계산을 **미리 해 두고 화면은 읽기만** 한다
      · `rebuildRowCache()` 파일 구성이 바뀔 때 (행을 훑는 무거운 것들)
      · `rebuildStatusCache()` 값 정리 상태가 바뀔 때 (컬럼별 상태·남은 일)
- [x] 첫 화면 미리보기 카드는 **앞 12컬럼만** 그린다 (113컬럼 × 9행 ≈ 1000칸을 매번 그리고 있었음)
      `나머지 101컬럼 보기` 버튼으로 큰 창 열기
- [x] 컬럼을 합치거나 행을 거른 뒤 **행 수 검사**(`verifyRowCount`) — 예상과 다르면 그 자리에서 알림
- 참고: 앱을 Debug로 돌리면 Swift가 최적화 없이 도는 만큼 더 느립니다. Release로 실행하면 확실히 빨라져요.

## “내 눈에는 111행인데 앱은 407행” — 숨겨진 행이었다
- 원인: 그 xlsx에는 **`hidden="1"`로 숨겨 둔 행이 296개** 있었다 (엑셀·넘버스에서 필터로 감춘 뒤 저장).
  화면에는 111행(전부 `Submitted`)만 보이는데, 우리 리더는 XML의 모든 행을 읽어 407행으로 셌다.
- [x] `XLSXReader`가 행의 숨김 여부를 읽는다 (`readGridWithHidden`)
- [x] `readVisibleTable(includeHidden:)` — 머리글 줄 자동 탐지 + **숨긴 줄은 빼고** 읽고,
      몇 줄을 뺐는지(`hiddenSkipped`) 함께 돌려준다
- [x] `PlanBuilder.passthrough(url:includeHidden:)` — 기본은 **보이는 대로**,
      `FilePlan.hiddenRowsSkipped`에 기록
- [x] 파일 칩: `111행 · 53컬럼 · 숨긴 행 296개 제외 · 눌러서 보기`
- [x] 합치기 스텝: `시트에서 숨겨 둔 행 296개는 빼고 읽었어요` + **`숨긴 행도 포함해서 다시 읽기`**
- [x] 행 수 대조표에도 `시트에서 숨겨져 있어 뺀 행 296행`
- 검증: 보이는 대로 **111행**(전부 `Submitted`) · 숨긴 행 포함 407행

### 숨긴 행은 조용히 빼기 (‘할 일’로 묻지 않기)
- [x] 합치기 단계에서 숨긴 행 안내 스텝 제거 — 숨긴 행은 **그냥 안 읽는다**
- [x] 행 수 대조표에만 조용한 한 줄: `시트에서 숨겨져 있던 행 (안 읽음) 296행 [포함하기]`
- [x] 세션에 `hiddenRowsAware` 표시 — 예전 세션(숨긴 행까지 읽힌 것)을 이어서 열 때
      이어서 하기 카드에 `예전 규칙으로 읽혀서 숨긴 행까지 들어 있을 수 있어요` 주의 문구

## 완성본 미리보기 스크롤 최적화
- 원인: 한 줄마다 **113개 셀을 전부** 그리고 있었고, 셀마다 배경 세 겹 + 좌우 강조선 +
  **툴팁(.help)** + 오른쪽 클릭 메뉴 + 더블클릭 제스처가 붙어 있었다.
  보이는 20줄이면 2000칸이 넘는다.
- [x] 행·머리글을 `LazyHStack`으로 — **화면에 보이는 열만** 그린다 (113 → 8칸 남짓)
- [x] 셀 배경 세 겹 → `cellBackground` 한 겹, 강조선은 오른쪽 하나만
- [x] 셀 툴팁 제거 (값은 더블클릭으로 크게 본다), 행 높이 고정으로 레이아웃 계산 줄임
- [x] 범례에서 없어진 표시(출처 점·쪼개진 컬럼) 정리, `틀 밖 컬럼` 항목 추가
- 확인: 실제 파일 3개(336행 · 113컬럼)로 창을 띄워 표가 정상 렌더링되는 것 확인

## 틀의 빈 칸부터 채우게 하기 + 틀 밖 컬럼 강조
### 빈 칸 채우기 카드 (`templateFillCard`)
- [x] 틀에는 있는데 값이 없는 칸을 **하나씩** 보여 준다 — `틀의 빈 칸 채우기 · 6개 중 1번째`
- [x] 그 칸에 넣을 만한 후보를 값으로 찾아 제시: `파일의 ‘휴대폰 번호’가 이 칸 같아요 (75%)`
      → **`‘휴대폰 번호’ 값 가져오기`** (칸만 옮김, 행은 그대로)
- [x] `다른 칸 고르기…`(파일별로 직접 지정) · `여러 칸 합치기…`(순서·구분자 설정) ·
      **`값 만들기…`** · `비워 두기`
- [x] `ValueGenerator` + `GeneratedValue` — 빈 칸을 **만들어서** 채운다
      · `모든 행에 같은 값` (예: POSTECH) · `번호 매기기` (첫 번호로 규칙 지정, 6F10001…)
      · **비어 있는 칸에만** 넣고 행 수는 그대로. 미리보기·내보내기 양쪽에 적용, 세션 저장
### 틀 밖 컬럼을 눈에 띄게
- [x] 컬럼 판을 **틀 밖 / 틀 안** 두 구역으로 — 틀 밖 구역에 주황 배너
      `틀 밖 컬럼 12개 — 이 값들은 틀 안의 칸으로 옮겨야 합니다` + `모두 고르기` · `짝지어 주기…`
- [x] 칩에 `틀 밖` 주황 뱃지 + 주황 테두리
- [x] 미리보기(인라인·큰 창): 틀 밖 컬럼 머리글을 **주황 배경 + `틀 밖 — 틀 안의 칸으로 옮기세요`**,
      열 전체 옅은 주황, 범례에 `틀 밖 컬럼 N개 — 틀 안으로 옮기세요`
- 검증: 옮기기(`연락처` → `Phone Number`) · 합치기(`성`+`이름` → `Korean Name` = `김 철수`) ·
  만들기(고정값 `POSTECH`, 번호 `6F10001·6F10002`) · 행 수 그대로

## ‘이 값 채우기’ — 어디서 → 어떻게
- [x] `FillColumnSheet` — 틀 안의 칸을 고르고 `이 값 채우기…`를 누르면
      ① **어디서**: 후보 칸을 **파일별로 묶어** 보여 준다 (파일 이름 · 값 예시 3개 · 추천 %).
         여러 개 체크 가능, 70% 이상 후보는 미리 골라 둠
      ② **어떻게**: 2개 이상 골랐을 때만 — 순서(↑↓)와 사이에 넣을 것(붙여쓰기·공백·쉼표·하이픈·직접),
         `이렇게 됩니다`에 실제 값으로 미리보기
      · 후보가 없으면 `값 만들기…`로 (고정값·번호 매기기)
- [x] 들어가는 곳 네 군데: 빈 칸 카드의 `이 값 채우기…` · 컬럼 판에서 한 개 골랐을 때 ·
      미리보기 창의 **머리글 오른쪽 클릭** · 셀 오른쪽 클릭
- [x] 미리보기 창에 `틀 밖 컬럼만 (12)` 필터 — 아직 안 옮긴 값들만 모아 볼 수 있다
- [x] 후보 목록은 틀 밖·빈 칸 위주로 (이미 제 몫을 하는 틀 안 칸은 빼고, 추천된 것은 포함)

## 변경 전후 보기 · 수정된 값 강조 · 예시 데이터 · 전체 컬럼 표시
- [x] `ChangeLogSheet` — 미리보기 창의 `변경 내역 N` 버튼으로 열리는 전후 비교 화면
      · 컬럼·이전 값·새 값이 같은 것끼리 묶어 `서울시 → 서울 · 12행`처럼 한 줄로
      · 컬럼 필터 · 값 검색 · 어떤 파일의 어느 행이었는지 · `복사`(탭 구분)
- [x] `PreviewPayload.changes` — 값 정리가 만든 `ChangeRecord`를 미리보기 모델까지 전달
- [x] 수정된 칸을 더 또렷하게 — 배경 파랑 0.20 + 파란 굵은 글씨,
      행 머리에 `수정 N` 뱃지 (그 줄에서 몇 칸이 바뀌었는지)
- [x] 검토가 필요한 컬럼에 **값 예시**를 함께 (`cache.samples`) — 칩과 ‘남은 일’ 카드에
      `도시 (3종) — 서울 · 서울시`처럼
- [x] 완성본 미리보기(첫 화면)에서 **컬럼을 더 이상 12개로 자르지 않는다** —
      가로도 `LazyHStack`이라 보이는 것만 그리면서 전체 컬럼을 옆으로 밀어 볼 수 있다

## 역할 정리: 앱 = 길잡이, 미리보기 = 작업대
- [x] 첫 화면에 **작업대를 그대로 끼워 넣음** — `PreviewWindowView(embedded: true)`.
      큰 창과 **완전히 같은 UI**(컬럼 체크·폭 조절·중복 뱃지·수정 강조·필터·변경 내역)이고
      컬럼도 전부(틀 밖 포함) 옆으로 밀어 볼 수 있다. `큰 창으로 열기`로 띄우면 같은 화면이 커진다
- [x] 앱 위쪽은 **길잡이·검증**으로:
      · `지금 상태` 줄 — `행 수 336 = 336 ✓` · `중복 14행 표시됨` · `빈 칸 N개` · `틀 밖 N개` ·
        `정리할 값 N컬럼` · `바뀐 값 N개` (누르면 그 일로 바로 이동)
      · `1. 합치기` 카드의 행 수 대조표와 확인 스텝
- [x] 작업대 맨 위에 **`지금 할 일`** 한 줄 — 급한 순서(틀 빈 칸 → 틀 밖 컬럼 → 값 정리 → 중복)로
      한 가지만 제시하고, 그 자리에서 `‘…’ 채우기…` 버튼으로 실행
- [x] 옛 인라인 표(별도 구현)는 삭제 — 화면이 두 벌로 갈라지지 않게
- 확인: 실제 파일 3개로 앱을 띄워 캡처 — 파일 칩 `111행 · 숨긴 행 296개 제외`,
  `행 수 336 = 336 ✓`, 작업대의 `지금 할 일` 줄까지 정상

## 목표를 바로잡음: 틀 밖을 없애는 게 아니라 틀 안의 행을 채우는 것
- [x] **‘남은 일’이 거짓말하던 문제** — 아직 아무 작업도 안 했는데 “이제 가져가면 돼요”가 떴다.
      *통째로* 빈 컬럼만 세고 있었던 탓. 이제 **틀 안 컬럼마다 비어 있는 행 수**를 센다
      (`computeTemplateHoles`): 실제 데이터로 재 보니 틀 안 67컬럼 × 336행 = 22,512칸 중
      **15,237칸이 비어 있었다** (32%만 참). 이게 진짜 남은 일이다
- [x] `지금 상태` 줄에 `틀 안 32% 참 · 빈 칸 15237` 진행률 추가. 누르면 가장 많이 빈 컬럼부터 채우기
- [x] `틀 밖 52개`(주황 경고) → **`틀 밖 재료 52개`(회색)**. 틀 밖은 없앨 대상이 아니라 틀 안을
      채울 재료다. `지금 할 일`에서도 틀 밖을 시키지 않는다
- [x] **`만들던 통합본에 이어붙이기…` 버튼 삭제** — 밖에서 가져오는 파일은 틀 하나뿐이고,
      틀은 **컬럼 이름만** 준다. 행은 올린 파일에서만 온다 (그래서 틀의 행 수는 비교 대상이 아님)

## 완성본 미리보기: 색으로 컬럼을 갈라 보여 주기
- [x] 컬럼 색 = 틀과의 관계 — **파랑: 틀 안인데 빈 행 있음 / 초록: 틀 안 다 참 / 회색: 틀 밖(재료)**
- [x] 셀 단위로 **빈 칸만 파랗게** — 어느 행을 채워야 하는지 표에서 바로 보인다
- [x] 머리글 둘째 줄에 `빈 행 225개 — 눌러서 채우기` (누르면 그 자리에서 ‘이 값 채우기’)
- [x] **틀 안 컬럼을 왼쪽에 먼저** 세운다 (틀이 정한 순서 그대로), 틀 밖은 뒤로
- [x] 보기 좁히기 토글 `채울 칸만 (66)` / `틀 밖 재료만 (52)`
- [x] 떠 있는 머리글 아래로 행이 비쳐 보이던 것 수정 (불투명 바닥 + 높이 정리)
- 확인: 실제 파일 3개 + 틀로 앱을 띄워 캡처 — 행 수 336 = 336 ✓ 유지, 색·순서·토글 정상

## 줄이 어긋나 보이던 것 · 글자가 세로로 접히던 것
- [x] **24행·28행만 오른쪽으로 밀려 그려지던 문제** — 줄마다 `LazyHStack`이 제 나름대로 너비를
      재는 탓이었다. 가로로 스크롤하던 중에 만들어진 줄은 앞쪽 자리 추정이 달라져 몇 px씩 어긋난다.
      이제 `tableWidth`(줄 머리 + 모든 컬럼 폭)를 계산해 **모든 줄과 머리글에 같은 너비를 못 박는다**
- [x] 창이 좁아지면 툴바·범례의 한글이 **세로로 접히거나 ‘…’로 잘리던 것** 수정:
      보기 옵션 체크박스 5개를 `보기 ▾` 메뉴 하나로 접고(켜져 있으면 ●), 남은 라벨은 전부
      `lineLimit(1).fixedSize()`, 범례는 가로 스크롤로

## 앱 = 진행도판, 미리보기 = 작업대 (역할을 끝까지 밀어붙임)
- [x] 첫 화면에서 **키 컬럼 고르기 · 틀 빈칸 채우기 · 컬럼 제안 · 컬럼 보드 · 끼워 넣었던 표**를 전부 내림
- [x] 대신 한 장의 **진행도 카드**:
      · “완성본 미리보기에서 채울 컬럼을 고르세요” + `완성본 미리보기 열기` 버튼
      · `틀 안 채움 32%` 막대 (7,275 / 22,512칸)
      · 큰 숫자 셋 — 더 채워야 할 칸 `15,237` · 값을 정리할 컬럼 `17개` · 중복으로 보이는 행 `14행`
- [x] 그 아래 **데이터 정합성** 카드만 남김 — 행 수 대조표, 숨긴 행, 키 컬럼 한 줄
- 확인: 실제 파일 3개 + 틀로 두 창을 함께 캡처 — 1~28행 어긋남 없음, 툴바 한 줄, 범례 한 줄

## 여러 컬럼을 고르고 ‘데이터 정리하기’ → 어디서 가져올지부터 묻기
- [x] 고른 것 중 **틀 안인데 빈 행이 있는 칸**이 있으면, 값 형식을 다듬기 전에
      `틀 안의 칸을 어디서 채울까요?` 창을 먼저 띄운다 (`FillFromSheet`)
      · 왼쪽 = 기준이 되는 틀 안 칸 + `336/336행 비어 있음`
      · 오른쪽 = 값을 가져올 컬럼 고르기 (닮은 정도 %와 예시 값을 같이 보여 줌)
      · 함께 고른 틀 밖 컬럼을 맨 위 후보로, 값 모양이 닮은 짝만 미리 골라 둔다
        (한 컬럼을 두 칸에 동시에 넣지 않게 이미 쓴 출처는 건너뜀)
      · `값 형식만 다듬기…`를 누르면 원래의 값 정리 화면으로 간다
- [x] 여러 칸을 한 번에 채울 때 **다시 만드는 건 마지막 한 번**만 (`mutatePlans` + `applyColumnFills`),
      끝나고 `verifyRowCount("칸을 채운")`으로 행 수가 그대로인지 확인
- 확인: 실제 파일 3개 + 틀로 창을 띄워 캡처 — 기준 3칸·후보 목록·예시 값·비활성 버튼까지 정상

- [x] `데이터 정리하기`(지팡이 아이콘) → **지금 누르면 일어날 일**을 그대로 쓴 이름으로 바꾸고 심볼을 뺌
      · 틀 안에 빈 칸이 섞여 있으면 `이 칸들 채우기…`
      · 아니면 `값 형식 맞추기…`
      · 아무것도 안 골랐을 때의 안내도 `머리글 네모 체크 → 채우기·합치기`

## 고친 값이 완성본 미리보기에 반영되지 않던 버그
- [x] 원인을 실제 파일로 재현해 확인 (헤드리스 테스트):
      ```
      고칠 컬럼: Korean Name | 황도희 → 황도희★
      patchColumns 비었을 때 : 반영 안 됨      ← 지금까지의 동작
      patchColumns 포함할 때 : 황도희★
      ```
      틀을 쓰면 결과물은 “쌓아 둔 원본 위에 **고른 컬럼만** 덮어쓰기”로 만들어진다.
      그런데 작업대에서 값을 바로 고쳐도 그 컬럼이 `focusColumns`에 없으면 덮어쓸 목록에서 빠져
      원본 값이 그대로 보였다
- [x] `focusOrdered`에 **실제로 값을 바꿔 둔 컬럼**을 자동으로 포함 — 고르지 않았어도 반영된다
- [x] 작업대에 넘기는 컬럼을 늘 `finalColumns` 전부로 (고른 컬럼만 남아 다른 컬럼이 사라지던 것도 수정)

## ‘이 컬럼 끝내기’ → 보고 · 적용하고 · 넘어가기
- [x] 컬럼 상세 화면 맨 아래를 셋으로 갈랐다
      · `변경사항 미리보기 (N)` — 이 컬럼에서 무엇이 무엇으로 바뀌는지 이전 값 → 새 값으로 (`ChangeLogSheet`)
      · `넘어가기` — 확정 표시 없이 다음으로 (고친 값은 그대로 남음)
      · `적용하기 (N) →` — 다 본 것으로 표시하고 다음 컬럼으로
      바뀔 값이 없으면 `바뀐 값 없음`(비활성) · `이대로 확정 →`으로 바뀐다

## 추상적인 이름 걷어내기 ②
- [x] `값 형식만 다듬기…` → 두 갈래로 나눠 구체적으로:
      · 줄마다 **`패턴으로 채우기…`** — 가져올 컬럼이 아예 없는 칸(지원연도·구분 같은 것)은
        모든 행에 같은 값을 넣거나 `6F10001`처럼 첫 번호를 적어 1씩 올라가는 번호를 만들어 넣는다
      · 아래쪽 **`이미 있는 값의 오타·표기 정리…`** — 값을 새로 가져오지 않고 이미 든 값만 손볼 때
- [x] `값 살펴보기` → **`변화 미리보기`**. 제목도 `‘국문 이름’ 변화 미리보기`,
      요약은 `바뀌는 값 3종 · 그대로 두는 값 193종`, 표 머리글은 `지금 값 (원본)` / `바뀔 값 (완성본)`,
      열자마자 **바뀌는 값부터** 정렬해서 보여 준다
- 확인: 실제 파일 3개 + 틀로 두 창을 캡처해 문구·정렬까지 확인

## 나뉜 컬럼 두 개를 틀 안 한 칸으로 (국문 성 + 국문 이름 → Korean Name)
- [x] 되긴 됐지만 머리글 오른쪽 클릭 메뉴에 숨어 있어서 못 찾았다. 이제 **표에서 세 개를 체크
      (Korean Name · 국문 성 · 국문 이름) → `이 칸들 채우기`** 만 하면 된다
- [x] 채우기 창에서 칸마다 **여러 컬럼을 고를 수 있게** 바꿈:
      · 고른 순서가 곧 붙는 순서 — `① 국문 성 ② 국문 이름` 칩을 ‹ › 로 옮기고 ✕로 뺀다
      · 사이에 넣을 글자 고르기 — 붙여쓰기 · 공백 · 쉼표 · 하이픈
      · 바로 아래 `이렇게 들어갑니다 → 김 철수` 로 실제 값을 붙여 보여 준다
- [x] 후보 메뉴를 `함께 고른 컬럼` / `그 밖의 컬럼`으로 나눠, 100개 넘는 목록에서 찾아 헤매지 않게
- [x] 첫 화면 진행도 카드에도 한 줄로 안내 — “성·이름처럼 둘로 나뉜 컬럼은 틀 안의 칸과 함께 체크한 뒤…”

## ‘작업한 게 저장이 안 되고 매번 초기화된다’ — 원인과 해결
저장은 멀쩡했다. 실제 세션 파일을 열어 확인:
```
stage: work | files: 3 | templateColumns: 67 | keyColumn: Code | base rows: 336
  파일0 Korean Name 출처: ['국문 성 (예: 김)', '국문 이름 (예: 애플)']
```
문제는 **되살릴 때**였다. `restore()`가 저장해 둔 결정(틀 컬럼·키 컬럼·번호 패턴·지운 행)을
읽어만 놓고 **결과물을 다시 만들지 않아서**, 기준선을 아무 옵션 없이 다시 쌓고 있었다.
- [x] 되살린 직후 `rebuildWorkColumns()`를 한 번 돌려 틀·키·패턴·지운 행까지 그대로 반영
- [x] 파일의 `숨긴 행 296개 제외`도 세션에 저장·복원 (표시가 사라졌던 것)
- [x] `이어서 하기` 카드를 드롭 상자 **위로** 올림 — 아래에 있어서 못 보고 파일부터 다시 올리게 됐다
- 확인: 채우기 → 앱 종료 → 되살리기. 되살린 화면에 `틀 안 채움 32% · 7,275/22,512칸`,
  `더 채워야 할 칸 15,237`, `값을 정리할 컬럼 15개`, `중복 14행`,
  `키가 비어 있던 225행엔 번호를 만들어 넣었어요`, 합쳐 둔 `Korean Name`까지 전부 그대로

## 앱을 켜면 바로 이어서
- [x] `이어서 하기` 카드를 없애고, 앱을 켜면 **저장해 둔 작업을 자동으로 이어 연다**
      (여는 동안 `지난 작업을 이어 여는 중…` 표시)
- [x] 대신 위쪽에 `새 작업 시작…` — 누르면 한 번 묻고, 올린 파일과 정한 것(틀·키·채운 칸·정리한 값)을
      저장본까지 싹 지우고 빈 화면으로
- 확인: 파일 3개 + 틀로 작업 → 앱 강제 종료 → **아무 것도 안 하고 앱만 실행**.
  `틀 안 채움 32% · 7,275/22,512칸`, `더 채워야 할 칸 15,237`, `키가 비어 있던 225행…`까지 그대로 열림

## 채우는 ‘방법’을 고르게 (남성/여성 + male/female 같은 경우)
이어 붙이기밖에 없어서, 두 칸을 한 칸으로 모으면 `남성 male`이 되어 버렸다.
- [x] `CombineMode` 추가 — **이어 붙이기**(성 + 이름 → 김 철수) / **값이 있는 것 하나만**
      (앞의 칸이 비어 있으면 다음 칸의 값을 쓴다)
- [x] 두 창 모두에서 고를 수 있게: `틀 안의 칸을 어디서 채울까요?`(칸마다) ·
      `여러 칸을 한 칸으로 합치기`(합치기 창). ‘사이에 무엇을’은 이어 붙이기일 때만 나온다
- [x] 두 창 모두에 **`합친 뒤에 이어서 값도 하나로 통일하기 (male → 남성처럼)`** 체크 —
      켜면 채우고 바로 그 컬럼의 값 정리 화면으로 넘어간다
- [x] 파일 계획과 세션에 방식을 저장 (되살려도 그대로)
- 확인 (헤드리스):
  ```
  이어 붙이기 (지금까지) → ["남성 male", "female", "여성", "(빈칸)"]
  값이 있는 것 하나만   → ["남성",      "female", "여성", "(빈칸)"]   행 수 4 → 4
  ```

## 완성본 미리보기에서 바로 내보내기·컬럼만 복사
- [x] `내보내기` 메뉴 — **엑셀 파일(.xlsx)** 또는 **CSV**로 저장.
      보기를 좁혀 뒀으면 그 행만, 머리글을 골라 뒀으면 그 컬럼만 나간다
- [x] `.xlsx` 쓰기를 직접 붙였다 (`XLSXWriter` + 압축 없는 `ZipWriter`).
      값은 전부 글자로 넣어 `0012`·`010-1234-5678`의 앞자리 0이 사라지지 않는다
- [x] CSV에는 BOM을 붙여 엑셀에서 한글이 깨지지 않게
- [x] `복사` 메뉴 — `고른 컬럼만 복사 (N)` · `고른 컬럼 이름만 복사` ·
      `보이는 표 전체` · `보이는 표 (행·출처 빼고)`
- 확인: 만든 xlsx를 이 앱의 읽기 코드로 다시 읽어 왕복 확인 (헤더 4개·3행 그대로,
  따옴표·`&`·`<태그>`·줄바꿈·앞자리 0 보존), ZIP 무결성 검사와 Quick Look 열기까지 통과

## 이름 칸을 성 + 이름으로 채우기 (받는 칸을 자유롭게)
- [x] 받는 칸을 ‘틀 안이면서 빈 행이 있는 컬럼’으로만 잡고 있어서, **이미 차 있는 ‘이름’ 칸**이나
      틀 밖 칸을 고르면 창이 빈 채로 떴다. 이제 순서대로 고른다:
      빈 행 있는 틀 안 칸 → 틀 안 칸 → 고른 것 전부 (그래서 빈 창은 없다)
- [x] 후보 목록의 ‘함께 고른 컬럼’에 **틀 안 컬럼도** 포함 (전엔 틀 밖만 올려서 성·이름이 안 보였다)
- [x] 컬럼을 **2개 이상 고르면 언제나** 채우기·합치기 창으로 — 버튼 이름도 `이 칸들 합쳐 채우기…`
- [x] 이미 차 있는 칸을 받는 자리로 쓰면 `지금 값은 새 값으로 바뀜`이라고 미리 알려 준다

## 줄이 어긋나 보이던 것 — 이번엔 구조를 바꿔서 해결
너비를 못 박는 것만으론 부족했다 (`LazyHStack`이 줄마다 ‘아직 안 그린 왼쪽 칸들’의 너비를
제 나름대로 어림잡기 때문). 이제 **그릴 컬럼 구간을 표 전체에서 한 번만 정한다**:
- [x] 가로 스크롤 위치와 창 너비로 `columnWindow`(구간 + 왼쪽 여백)를 계산
- [x] 모든 줄과 머리글이 그 구간만, 같은 여백으로 그린다 (`LazyHStack` → `HStack`)
- [x] 눌러서 데려갈 컬럼은 구간에 강제로 포함해 `보는 중:` 이동이 계속 동작
- 확인: 세로·가로로 끝까지 굴려 캡처 — 머리글과 모든 줄이 정확히 맞고 줄 높이도 일정

## 오른쪽 컬럼이 통째로 안 보이던 것
줄 어긋남을 잡으려고 ‘그릴 컬럼 구간’을 도입했는데, **그 구간이 늘 0..<11에 머물렀다.**
화면에 디버그 값을 찍어 확인: `off=0` — 가로 스크롤 위치가 뷰까지 올라오지 않았다
(`GeometryReader` + `preference`로는 값이 안 잡힘).
- [x] 스크롤 위치를 **AppKit 스크롤뷰(NSClipView)의 bounds에서 직접** 읽도록 교체
      (`ScrollOffsetReader` — `boundsDidChangeNotification` 구독). 붙었는지도 확인 (`known=1`)
- [x] 만약 스크롤 위치를 못 읽는 상황이면 **컬럼을 전부 그린다** —
      느려질지언정 컬럼이 안 보이는 일은 없게
- [x] 좌우 여유 칸을 3·4개로 늘리고, 구간 계산에서 배열 할당을 없앰 (줄마다 도는 자리라서)

- [x] 셋(틀 안 1 + 틀 밖 2)을 골라 ‘이 칸들 합쳐 채우기’를 눌렀는데 **한 줄만 보이던 것** —
      한 줄이 맞다(값을 *받을* 칸이 하나니까). 다만 함께 고른 둘이 메뉴 안에 숨어 있어
      아무것도 안 고른 것처럼 보였다. 이제 **받을 칸이 하나면 나머지를 그대로 재료로 넣어 둔다**:
      창을 열자마자 `국문 성 + 국문 이름`, `① … ② …` 순서 칩, `사이에 공백`,
      `이렇게 들어갑니다 → 민 건우`까지 채워져 있다
- [x] 안내 문구도 “나머지 2개(국문 성 · 국문 이름)는 여기에 넣을 재료입니다”로 분명하게
- [x] 함께 고른 컬럼은 값 예시를 못 뽑아도 후보 목록에서 빼지 않는다

## 한 컬럼의 값을 다른 컬럼으로 통째로 옮기기
- [x] 완성본 미리보기에서 컬럼 머리글(또는 셀)을 오른쪽 클릭 →
      **`‘…’ 값을 다른 컬럼으로 옮기기…`** 하나로 끝나게 (`MoveColumnSheet`)
      · 어느 컬럼으로 — 틀 안 먼저, 비어 있는 행이 많은 순서로 목록
      · 어떻게 — 셋 중 하나 (아래 표 참고)
      · `이렇게 됩니다 → …` 로 실제 값 미리보기, 옮기면 원래 컬럼은 없어진다
- 확인 (헤드리스, 행 수 4 → 4 그대로):
  ```
  받는 칸(이름)      : 김철수 · (빈칸) · 박민수 · (빈칸)
  옮길 칸(국문 이름) : 철수   · 영희   · (빈칸) · (빈칸)
  빈 칸만 채우기 →   김철수 · 영희 · 박민수 · (빈칸)
  이어 붙이기   →   김철수 철수 · 영희 · 박민수 · (빈칸)
  통째로 바꾸기 →   철수 · 영희 · (빈칸) · (빈칸)
  ```

## 한 줄만 값이 통째로 밀려 보이던 것 — CSV 파서 버그
그림 문제가 아니라 **읽기가 틀렸다.** 원본 줄은 멀쩡했다:
```
…,Checked,박‍시현,‍시현,박,tlgus0929@hufs.ac.kr,+82 10-2694-9314,…
```
앱이 읽은 값: `Korean Name = 박‍시현,‍시현` — 쉼표를 못 나누고 두 칸을 붙였다.
- [x] 원인: 파서가 **글자(Character) 단위**로 읽고 있었다. Swift의 Character는 자소 묶음이라,
      쉼표 뒤에 결합 문자(U+200D)가 오면 `","`와 U+200D가 **한 글자로 묶여** 쉼표로 인식되지 않는다.
      그 줄부터 값이 한 칸씩 밀렸다
- [x] **유니코드 스칼라 단위**로 읽도록 고침 (RFC-4180 그대로)
- 확인: 그 줄만 따로 — 7칸이 제자리에 들어감. 실제 파일 전체(65컬럼 · 248행)에서
  **이메일 칸에 이메일이 아닌 값이 든 행 0건** (고치기 전 1건)
- [x] `데이터 정합성` 카드에 **`파일 다시 읽기`** 추가 — 이미 읽어 둔(=잘못 읽힌) 값을
      원본에서 새로 읽어 온다. 정해 둔 컬럼 구성(합친 칸·구분자·방식)은 그대로 이어 간다

## 미리보기가 텅 비어 보이던 것
값을 고치면 표를 다시 만드는데, 그때 스크롤이 0으로 돌아가도 **기억해 둔 가로 위치는 그대로**라
컬럼을 화면 밖에 그리고 있었다.
- [x] 가로 위치를 기억하지 않고 **그릴 때마다 스크롤뷰에 직접 물어보도록** 바꿈 (`TableScroll`)
- [x] 표가 다시 만들어져 스크롤뷰가 바뀌어도 매번 다시 이어 붙인다

## 모든 행 확정
- [x] 툴바의 `확정 N / 336행`을 메뉴로 — `보이는 행 모두 확정` · `전체 N행 모두 확정` ·
      `보이는 행 확정 해제` · `확정 전부 해제`

## ‘눌러도 아무 반응이 없다’ — 창이 안 뜨던 진짜 이유
`.sheet`를 한 뷰에 **14개** 겹쳐 달아 뒀다. SwiftUI는 이러면 뒤에 단 것을 조용히 무시한다.
- [x] 창을 **하나로 모았다** (`SheetKind` + `activeSheet` + `sheetContent`) —
      어느 창을 띄울지는 우선순위로 정하고, 닫으면 관련 상태를 한 번에 비운다

## 컬럼마다 할 일을 틀과의 관계로
- [x] **틀 밖 컬럼** → `틀 안으로 보내기…` (어느 칸으로 보낼지 묻는 창)
- [x] **틀 안 컬럼** → `이 칸 채우기…` (틀 밖 어디서 가져올지 묻는 창).
      후보 정렬도 틀 안을 채울 땐 **틀 밖 컬럼을 위로** 올린다
- [x] 값이 차 있는 컬럼 머리글에 **`값 정리`** 링크 — 오타·형식을 바로 손보러 간다
      (정리할 값이 남았으면 `정리할 값 N종`으로 주황색)
- [x] 통째로 빈 컬럼도 ‘채울 것’으로 친다 (틀 밖이라 빈 행 수를 세지 않던 컬럼 포함)

## 설치용 패키지
- [x] `scripts/package.sh` — Release 빌드 → 서명 → **끌어다 놓는 .dmg** 한 번에
      (`dist/DataWizard-1.0.dmg`, 2.8MB). 이미지 안에 `데이터 마법사.app` · `Applications` 바로가기 ·
      `처음 열 때 읽어 주세요.txt`
- [x] 서명은 지금 가진 개발용 인증서로. **애플 공증은 못 받았다** (Developer ID 인증서가 없음)
      → 받는 사람은 처음 한 번만 오른쪽 클릭 → 열기. 안내문을 이미지에 넣어 뒀다
- [x] `dist/`는 저장소에 올리지 않는다 (.gitignore)

## 옮기기와 정리를 버튼으로 갈라 놓기
- [x] 컬럼을 고르면 툴바에 **버튼이 둘** 나온다 — 무엇이 일어날지 이름만 봐도 알게:
      · `틀 안으로 보내기…` / `여기 값 채우기…` / `한 칸으로 합치기…` — **값이 자리를 옮긴다**
      · `오타·형식 정리…` — **값을 그 자리에서 다듬는다** (같은 뜻 다른 표기 모으기, 전화번호·날짜 형식)
- [x] 아무것도 안 골랐을 때 안내도 `머리글 네모 체크 → 옮기기 · 정리`

## 매핑표를 붙여넣었는데 매핑이 안 되던 것
표는 맞았다. **값이 두 컬럼에 나뉘어 있었다** (실제 파일 확인):
```
[18] 대학교 재학/휴학/졸업 예정 — 대학교 1~4학년(222) · 휴학중(8) · 대학원 석사(2) · 박사(1)
[19] 그 외                  — 무직/구직중(8) · 풀타임 근로자(4) · 인턴쉽(2) · 프리랜서(1)
```
한 컬럼에서 표를 붙여넣으면 나머지 절반은 ‘이 컬럼에 없는 값’이라 아무 일도 하지 않는다.
- [x] 매핑 창에 **`이 컬럼에 없는 값 N줄`**을 주황색으로 표시 (툴팁에 그 값들)
- [x] 그 경우 안내 한 줄 — “값이 두 컬럼에 나뉘어 있다면, 먼저 두 컬럼을
      ‘값이 있는 것 하나만’으로 한 칸에 모은 뒤 이 표를 붙여넣으세요”

## 매핑이 안 먹던 진짜 이유 — 껍데기 컬럼
사용자 세션을 열어 확인:
```
'Current Status' ← ['대학교 재학/휴학/졸업 예정 '] | 방식: first     ← 값은 여기로 옮겨져 있었다
valueMap['대학교 재학/휴학/졸업 예정'] = 13개 규칙                   ← 규칙은 빈 컬럼에 걸려 있었다
행에 들어 있는 값: {'': 248}                                      ← 그 컬럼은 값이 0종
```
컬럼을 옮길 때 파일 헤더 이름이 **`대학교 재학/휴학/졸업 예정 `(뒤에 공백)** 인데
컬럼 이름(공백 뗀 것)으로 지우고 있어서, 값 없는 **껍데기 컬럼**이 목록에 그대로 남았다.
거기에 매핑표를 붙여넣으니 `전체 0종 · 매핑 0종` — 아무 일도 일어나지 않았다.
- [x] 옮길 때 **파일의 진짜 헤더 이름**으로 지운다 (앞뒤 공백까지)
- [x] 허용 목록(매핑표가 정한 값들)도 옮긴 칸으로 함께 이동
- [x] 이미 생겨 버린 껍데기는 **자동으로 치운다** (`pruneGhostHeaders`) —
      거기 걸려 있던 정리 규칙은 지금 그 값을 들고 있는 칸으로 옮겨 준다
- 확인 (헤드리스): 옮긴 뒤 껍데기 헤더 없음, 옮긴 칸에서 매핑 적용됨
  (`University Yr3` 79건 · `Off-Year (University)` 8건)

## “복사하면 값이 잘린다” — 잘린 건 화면이지 값이 아니다
붙여넣은 것을 원본과 대조:
```
복사된 값 248줄 · 가장 긴 값 'Korea Advanced Institute of Science & Technology (KAIST)' 56자 (온전)
```
표에서 `컴퓨터학부 플랫폼SW&데이터과학…`처럼 보이는 건 **컬럼 폭** 때문이고,
복사·내보내기에는 늘 값 전체가 나간다.
- [x] **폭 맞추기** 추가 — 머리글 오른쪽 끝 손잡이를 **두 번 누르면** 값에 맞춰 넓어진다
- [x] 오른쪽 클릭 메뉴·`보기` 메뉴에도 `이 컬럼 폭을 값에 맞추기` / `모든 컬럼 폭을 값에 맞추기`
